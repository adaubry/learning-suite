# DECISIONS.md — Journal des arbitrages pris en cours de route

> Complète ARCHITECTURE.md §11 (ADR fondateurs, actés avant implémentation). Ce fichier consigne les décisions prises **pendant** le développement : déviations, arbitrages ambigus tranchés avec l'humain, écarts par rapport à un document existant (AGENTS.md §1). Une ligne par décision, ordre chronologique, jamais réécrite rétroactivement.

| Date | Bloc / contexte | Décision | Raison | Alternative rejetée |
| ---- | ---------------- | -------- | ------ | -------------------- |
| 2026-07-06 | Bloc 4.1 — L2+S3 | Phase 3 (L0 béton, L1/sectionnement) n'existait pas encore dans le code au moment d'attaquer Bloc 4.1 (dernier bloc livré : 2.2). Construction d'un **L0 minimal** dans la même session (`callLLM`, `models.ts`, chargement `prompts/*.vN.md`, retry+PromptLog), strictement ce qu'exige L2 — pas de L1/sectionnement, pas de streaming. `callLLM` accepte un `validate?: (data) => string \| null` en plus du schéma zod, réutilisant la même boucle de retry pour les erreurs métier (couverture des gras) que pour les erreurs de schéma — généralisation justifiée par le besoin identique documenté pour L1 (bornes incohérentes ⇒ retry, PLAN Bloc 3.2). | Tranché avec l'humain (AskUserQuestion) plutôt que de bloquer la session ou de coder L2 en dur hors L0 (aurait violé l'invariant « OpenRouter uniquement via L0 »). | Bloc 3.1 dédié avant 4.1 (repousse la session) ; L2 appelant OpenRouter directement sans L0 partagé |
| 2026-07-06 | Outillage tests (hors bloc) | `evals/` (racine) déplacé vers `e2e/evals/` ; artefacts Playwright (`test-results/`, `playwright-report/`) sortent aussi sous `e2e/`. ADR 16 (ARCHITECTURE.md v0.4). | Un seul dossier pour tout ce qui touche aux tests e2e/ia, sans confusion avec les tests unitaires colocalisés dans `src/` ; les deux workflows (Playwright vs evals LLM) ne s'interfèrent pas nativement — `evals/run.mjs` ne matche pas le pattern `*.spec.ts` de Playwright. | `src/evals/` (abandonné par le plan) ; `evals/` séparé à la racine |
| 2026-07-06 | Schéma Drizzle complet (ARCHITECTURE §8) | Pas de table `User` domaine : tous les `user_id` référencent `auth.users(id)` (Supabase) directement via `pgSchema("auth")`, déclarée dans `src/db/schema.ts` avec un seul champ `id` (façade de typage/FK, pas gérée par les migrations Drizzle). | Éviter de dupliquer un état déjà possédé par Supabase Auth ; cohérent avec le stub existant et la doctrine mono-utilisateur (ARCHITECTURE §1.6). | Table `User` séparée synchronisée avec `auth.users` |
| 2026-07-06 | Schéma Drizzle complet — invariant « session unique » | Ajout de l'entité `StudyCycle` (état persisté machine B/C, `closed_at`) absente d'ARCHITECTURE §8 v0.3 ; porte l'invariant via index unique partiel `UNIQUE(user_id) WHERE closed_at IS NULL` et sert la reprise de session. ARCHITECTURE.md et FUNCTIONS.md mis à jour en conséquence (v0.4). | Un simple flag sur `StudySession` aurait confondu tentative immuable (ADR 6) et cycle multi-tentatives ; une table pointeur seule aurait laissé la reprise de session non résolue. | Statut ouvert/fermé sur `StudySession` ; table pointeur `OpenSession` minimale |
| 2026-07-06 | Migration `0000_nervous_meltdown.sql` | Suppression manuelle du `CREATE TABLE "auth"."users"` généré par `drizzle-kit generate` (la table existe déjà côté Supabase avec plus de colonnes) ; les FK vers `auth.users(id)` sont conservées. | `drizzle-kit` ne sait pas distinguer une table réellement possédée d'une table référencée pour son typage (`schemaFilter`/`tablesFilter` ne s'appliquent qu'à `introspect`/`push`, pas `generate`) — édition manuelle nécessaire à chaque régénération touchant `auth.users`. | Laisser drizzle-kit créer la table (échoue, la table existe déjà avec un schéma différent) |
| 2026-07-06 | `package.json` — script `check` | `check` et `test:canary` chargent désormais `.env.local` (`node --env-file=.env.local`) avant `vitest run`, comme `db:generate`/`db:migrate` le faisaient déjà. | Le nouveau test `src/db/schema.test.ts` (contraintes DB) a besoin de `DATABASE_URL` ; sans le flag, `vitest run` retombait sur la résolution Postgres par défaut du système (échec d'authentification). | Charger l'env via un fichier de setup Vitest (`setupFiles`) — rejeté, changement plus large que nécessaire pour un besoin ponctuel |
| 2026-07-06 | P0 É0.1 — Auth | Supabase **local** (CLI + Docker) plutôt qu'un projet hosted, pour cette phase. `supabase/config.toml` : `site_url`/`additional_redirect_urls` alignés sur `http://localhost:3000` (le défaut `127.0.0.1` bloquait silencieusement `emailRedirectTo`, repli sur `site_url` sans erreur). Fichier `middleware.ts` renommé `proxy.ts` (Next.js 16 déprécie la convention `middleware` — AGENTS.md exige de suivre les dépréciations de `node_modules/next/dist/docs/`) ; TECH_MAPPING.md mis à jour en conséquence. | Local = magic links captés par Mailpit (API HTTP), donc le parcours complet (créer un compte / connexion / déconnexion) est automatisable par Playwright sans vrai envoi d'email ; hosted aurait bloqué ce test. | Projet Supabase hosted (email réel, parcours non automatisable en l'état) |
| 2026-07-06 | P0 É0.2 + S9 partiel — Nav U1 | Le sidebar U1 affiche les 4 entrées (Aujourd'hui/Curriculum/Erreurs/Réglages) prévues par USER_FLOW dès ce bloc, mais Erreurs et Réglages sont **désactivées** (pas encore construites). Tranché avec l'humain (AskUserQuestion). | Respecter la navigation permanente du document sans faire miroiter des écrans fonctionnels qui n'existent pas encore ; réactivation triviale (retirer `disabled`) quand P5/P7 seront construits. | Ne lister que les 2 entrées actives ; lister les 4 en liens morts vers des pages stub |
| 2026-07-06 | S9 partiel — CRUD matières | Suppression dure hors scope ce bloc : seuls create/update/archive/unarchive sont implémentés (`src/services/account.ts`). | É6.4 (suppression, confirmation forte, cascade) est un mécanisme du bloc curriculum complet, pas de S9 partiel ; aucun chapitre n'existe encore donc rien ne serait perdu à archiver plutôt que supprimer. | Implémenter un `deleteSubject` minimal dès maintenant |
| 2026-07-06 | S9 partiel — détection onboarding fait | Pas de colonne dédiée : `hasCompletedOnboarding` teste l'existence d'une ligne `PlannerConfig` pour l'utilisateur (créée par `finishOnboardingAction` même si l'étape rythme a été passée, avec la valeur par défaut). | Éviter une colonne dont la seule utilité est un booléen d'état UI, alors qu'une donnée déjà persistée (le rythme, toujours écrit en sortie du wizard) suffit à le déduire. | Colonne `onboarding_complete BOOLEAN` sur un futur profil utilisateur |
| 2026-07-06 | `e2e/auth-flow.spec.ts` (test existant, non `@canary`) | Mis à jour pour refléter le nouveau parcours post-connexion : l'ancien test atterrissait sur l'ancienne page d'accueil du template Next.js (`app/page.tsx`, supprimée) et cherchait `[Se déconnecter]` en pleine page ; il traverse désormais l'onboarding (1 matière, étapes 2-3 passées) puis retrouve le bouton dans le sidebar U1. `.dispatchEvent("click")` remplace `.click()` sur tous les boutons : le dev overlay de Next 16 (portail plein écran) intercepte par intermittence les clics réels de Playwright en mode `next dev`. | Le test n'est pas `@canary`, mais son hypothèse (page d'accueil = scaffold Next.js) ne survivait pas à ce bloc ; signalé plutôt que contourné silencieusement. | Supprimer le test plutôt que le mettre à jour |
| 2026-07-06 | `package.json` — script `test:canary` (bug) | Le filtre Vitest était un glob (`"src/**/*.canary.test.ts"`), mais l'argument positionnel de `vitest run` est une regex/sous-chaîne, pas un glob — il ne matchait donc jamais aucun fichier (`No test files found, exiting with code 0`), silencieusement vert depuis la création du script car aucun `*.canary.test.ts` n'existait encore. Corrigé en `".canary.test.ts"`. Découvert en ajoutant le premier canari Vitest (`src/db/schema.canary.test.ts`). | Un `test:canary` qui ignore silencieusement tous ses tests est pire que l'absence de canari : faux sentiment de sécurité. | Garder le glob et lister les fichiers explicitement dans le script |
| 2026-07-06 | Schéma — invariant « aucune étude sans rubrique valide » | FUNCTIONS §7 documentait déjà « contrainte DB » comme renfort de cet invariant (propriétaire S3), mais aucune contrainte n'existait en base. Ajout d'un trigger Postgres (`0001_study_cycle_requires_valid_guide.sql`) : `BEFORE INSERT ON study_cycle` vérifie qu'un `CorrectionGuide.statut = 'valide'` existe pour la section, sinon `RAISE EXCEPTION`. | Un `CHECK` ne peut pas référencer une autre table en Postgres ; le trigger est le seul moyen d'un vrai renfort DB (et non juste applicatif dans S3/S4) pour cet invariant. Écart entre la doc et le code détecté en écrivant le canari demandé. | Laisser l'invariant purement applicatif (S3/S4), sans renfort DB malgré ce que dit FUNCTIONS §7 |
| 2026-07-06 | Bloc 2.1 — P1/P2/P3, fixtures | PLAN.md prévoit 3 vrais exports Google Docs sous `evals/fixtures/` ; seuls 2 existaient (`Introduction obli.md`, `Introduction contrat.md`), non trackés à la racine de `e2e/`. Déplacés vers `e2e/evals/fixtures/` (cohérent avec ADR 16) ; le bloc démarre avec 2 fixtures, le round-trip canary (`parseChapter.canary.test.ts`) reste ouvert au 3ᵉ export dès qu'il existe. | Signalé à l'humain en récitation (Étape 0) ; l'humain a validé la poursuite ("go") sans trancher explicitement le chemin/le compte — décision prise par défaut plutôt que de bloquer sur un détail de fichiers, réversible. | Attendre le 3ᵉ export avant de commencer le bloc |
| 2026-07-06 | P1 parseChapter — gras/italique dans les titres | FORMAT §2.2/§2.3 ne précise pas le cas d'un titre contenant lui-même du gras (rencontré dans les vrais fixtures : `### **Deuxième classification...**`). Décision : les titres sont purement structurels (§2.1 « aucun niveau ne déclenche de comportement spécial ») — P1 n'extrait aucun `boldSegment`/`italicSegment` depuis le texte d'un titre, seul son texte brut alimente l'arbre. | Éviter qu'un gras stylistique de titre (habitude Google Docs) pollue les segments destinés à la génération de rubrique, alors que rien dans FORMAT ne suggère qu'un titre puisse être un point de correction. | Extraire aussi le gras/italique des titres comme des segments ordinaires |
| 2026-07-06 | P1 parseChapter — groupement des paragraphes gras (§2.4) | FORMAT §7 : la prose dit que le 3ᵉ paragraphe gras de l'exemple est séparé par « deux lignes vides » (segment indépendant), mais le bloc de code montré dans FORMAT.md n'a qu'une ligne vide à cet endroit — incohérence interne au document (probablement un artefact de mise en forme lors de la rédaction). L'implémentation suit la règle textuelle de §2.4 (une ligne vide = fusion, deux = indépendant), pas le bloc de code tel qu'écrit. Signalé, non corrigé dans FORMAT.md par cette session. | La règle énoncée en prose est répétée et non ambiguë (§2.4 + FUNCTIONS §1) ; le bloc de code semble être l'erreur, pas la règle. | Faire correspondre l'implémentation au bloc de code (fusionnerait alors les 3 paragraphes en un seul segment, contredisant la prose du même document) |
| 2026-07-06 | Bloc 2.2 — S1.import + U23/U3/U5 | USER_FLOW É1.2 prévoit qu'un import validé enchaîne automatiquement sur É1.3 (sectionnement LLM 1). L1/S2 n'existent pas avant la Phase 3 : le wizard s'arrête à la création du `Chapter` v1 et redirige vers `/curriculum`. `[Corriger le texte]` (mini-éditeur inline, U4/Tiptap) reporté à la Phase 8 : bouton placeholder désactivé sur l'écran de rapport. « Création de matière à la volée » (É1.0) non reprise dans le wizard : sélection d'une matière existante uniquement (l'onboarding en garantit ≥ 1), création réservée à `/curriculum` (S9). | Signalé et validé par l'humain en récitation (Étape 0) avant implémentation. | Bloquer le bloc en attendant L1/S2 ou U4 ; dupliquer le formulaire de création de matière dans le wizard |
| 2026-07-06 | `vitest.config.ts` — `hookTimeout` | 5 fichiers de test tapent désormais Postgres local en parallèle (chacun son client `postgres()`) ; le nettoyage `afterAll` a occasionnellement dépassé les 10s par défaut sous contention (observé lors du bloc 2.2, ajout de `chapter.test.ts`/`audit.test.ts`). `hookTimeout` porté à 20000ms. | Éviter un `test:canary`/`check` rouge par simple lenteur de nettoyage plutôt que par régression réelle — confirmé stable sur plusieurs runs consécutifs après le changement. | Réduire le nombre de clients Postgres (un seul partagé entre fichiers) — changement plus large que nécessaire pour ce bloc |
| 2026-07-06 | Bloc 4.2 — U14 + clôture Phase 4 | Cinq écarts tranchés avec l'humain (AskUserQuestion) avant code : (1) Phase 3 (sectionnement) toujours absente — le test navigateur de Phase 4 sème les sections via SQL direct (`db:studio` en usage réel), aucune mini-UI de tri construite ; (2) le bouton manuel « générer la rubrique », promis par le bloc 4.1 mais jamais câblé, est câblé maintenant sur le curriculum (avec `[Rédiger manuellement]`, secours si le LLM échoue) ; (3) `GuideService` n'avait aucune vérification de propriétaire — ajout d'`assertSectionOwnership` (même pattern que `ChapterService.importChapter`), désormais requise par `generate`/`validate`/`regenerate`/`generateBatch`/`createManual` ; (4) le garde-fou « ≥ 1 point critique » (FUNCTIONS §7) est déplacé de la génération LLM vers `validate`, seul point où la section devient `prete` — couvre aussi bien l'édition manuelle des points que la rédaction depuis zéro ; (5) `createManual` insère une rubrique `a_valider` vide (`{points: []}`), remplie ensuite dans U14. Conséquence d'implémentation (non re-demandée) : `validate(userId, guideId, points)` persiste les points édités ET valide en une seule transaction — pas de fonction `updatePoints` séparée, l'édition reste côté client (`useState`) jusqu'à la soumission finale. | Root-cause unique par invariant (AGENTS §2.6) plutôt qu'un garde dupliqué génération+édition ; éviter un état intermédiaire persisté (« brouillon ») que rien dans USER_FLOW ne demande. | `updatePoints` + `validate` séparés (persistance intermédiaire) ; garder le garde-fou uniquement à la génération (contournable en supprimant le point critique après coup) |
| 2026-07-07 | Bloc 3.1 — L0 | `client.ts` ne gérait ni les échecs réseau (`fetch` rejeté) ni les réponses HTTP non-2xx d'OpenRouter — remontaient en exception non catchée ou étaient mal classés en « réponse non-JSON du modèle » et réinjectés au modèle. Ajout d'une distinction erreur infra (réseau/HTTP, retry sans réinjection de texte — rien à « corriger » pour le modèle) vs erreur de contenu (réinjectée comme avant). `response.ok === false` plutôt que `!response.ok` : un canari existant (`generateGuide.canary.test.ts`) mocke des réponses sans champ `ok`, un vrai `Response` fixe toujours ce booléen explicitement. | L0 « béton » (PLAN Bloc 3.1) exige de ne jamais laisser une panne d'infra se faire passer pour une faute du modèle ni crasher hors de la boucle de retry/PromptLog ; ne pas modifier le canari existant (AGENTS §2.5). | Garder `!response.ok` (aurait cassé le canari) ; ajouter une colonne d'erreur détaillée à `prompt_log` (pas demandé, le statut ok/retry/echec existant suffit) |
| 2026-07-07 | Bloc 3.2 — L1 + P6 | Format des bornes proposées par le sectionnement : le modèle ne manipule que des **indices 1-based dans le plan** (candidats = Titre 2, sinon Titre 1, listés dans le prompt), jamais d'offsets caractère. Le code résout les bornes réelles depuis `TitleNode.start/end` (déjà calculées par P1) une fois la couverture validée (partition ordonnée et complète de 1..N, sans trou ni chevauchement). Tranché avec l'humain (AskUserQuestion) — recommandation retenue. | Les LLM comptent mal les caractères ; une résolution déterministe côté code élimine toute la classe d'erreurs de désalignement, et réutilise directement les bornes déjà justes de P1 plutôt que de réinventer un remappage texte-sans-italiques → markdown brut. | Offsets caractère bruts renvoyés par le modèle (contrat plus proche de la lettre de FUNCTIONS.md, mais fragile et nécessite un remappage de coordonnées une fois les italiques retirées) |
| 2026-07-07 | Bloc 3.2 — L1 | « Chapitre trop grand ⇒ traitement par Titres 1 avec continuité » (FUNCTIONS §2 L1) **non implémenté** : aucun seuil n'est défini dans FUNCTIONS/ARCHITECTURE, et `LLM_CONTRACTS.md` qu'ils référencent (budgets chiffrés) n'existe pas dans le dépôt. Un seul appel LLM sur le chapitre entier pour l'instant, quelle que soit sa taille. Tranché avec l'humain (AskUserQuestion) — reporté. | Inventer un seuil maintenant aurait été deviner une exigence produit (AGENTS §6) ; les 2 seules fixtures réelles disponibles sont courtes, aucun cas réel ne force encore la décision. | Choisir un seuil arbitraire (tokens/caractères) et implémenter la cascade dès ce bloc |
| 2026-07-08 | Bloc 3.3 — S2 + U13, récitation | « S1.import … déclenche S2 » (FUNCTIONS §3 S1) ne veut pas dire un appel synchrone dans `chapter.importChapter` : USER_FLOW É1.3 exige un écran d'attente avec sa propre progression, incompatible avec un bouton « Valider l'import » bloqué le temps du LLM. `importChapter` reste inchangé (crée seulement le `Chapter`) ; le wizard redirige vers `step=sectionnement` et c'est l'arrivée sur cet écran qui déclenche la Server Action de `S2.propose` (streaming), même famille de pattern que `analyzeAction` à l'étape "import". | Tranché avec l'humain (AskUserQuestion) en récitation, avant code. | Appel synchrone de `S2.propose` dans `importChapter`, écran de sectionnement purement passif |
| 2026-07-08 | Bloc 3.3 — S2 + U13, récitation | TECH_MAPPING §4.3 (U13) mentionnait « réordonnancement comme U10 », absent de FUNCTIONS §6.2 et de USER_FLOW É1.4. Corrigé dans TECH_MAPPING v0.3 : un document de délégation technique n'a pas vocation à créer une exigence produit. U13 n'aura pas de réordonnancement manuel — l'ordre du tri suit celui des bornes proposées par L1/P6. | Erreur reconnue par l'humain en récitation, avant code (AGENTS §1 : contradiction entre documents signalée, jamais résolue en silence). | Construire le réordonnancement pour honorer TECH_MAPPING au lieu de corriger le document |
| 2026-07-08 | Bloc 3.3 — S2 + U13, récitation | USER_FLOW É1.3 demandait « bornes visualisées sur une minicarte du document » en plus de l'aperçu dépliable de U13 — pièce d'UI non triviale absente de FUNCTIONS/TECH_MAPPING. Abandonnée, retirée de USER_FLOW v0.4 (pas reportée dans un autre document) : la liste ordonnée + aperçu dépliable suffisent à juger le sectionnement. | Tranché avec l'humain en récitation, avant code. | Construire la minicarte ce bloc ; la reporter sans corriger USER_FLOW (aurait laissé une exigence documentée mais jamais construite) |
| 2026-07-08 | Bloc 3.3 — S2.propose | `propose` supprime les sections `a_trier` existantes du chapitre avant d'insérer la nouvelle proposition (plutôt que d'échouer ou d'empiler des doublons) : l'écran É1.3 rappelle cette action à chaque montage (StrictMode, rechargement, `[Relancer]`) et ces lignes n'ont encore aucune dépendance (pas de rubrique, pas d'étude) tant qu'elles sont `a_trier` — `active`/`exclue`/`archivee` ne sont jamais touchées par `propose`. | Rendre le bouton `[Relancer]` (USER_FLOW règle 1) réellement rejouable sans duplication, sans construire un mécanisme de génération différentielle hors scope de ce bloc. | Refuser un second appel à `propose` tant que des `a_trier` existent (bloquerait `[Relancer]` et un simple rechargement de l'écran) |
| 2026-07-08 | Bloc 3.3 — S2 + U13, découverte en test navigateur | Bug découvert en testant l'écran de sectionnement dans un vrai navigateur : le `useEffect` qui déclenche `S2.propose` à l'arrivée sur É1.3 s'exécutait deux fois (double-invocation React en dev) avant que l'état `pending` ne se propage, lançant deux appels LLM concurrents (~4 min chacun avec le modèle configuré). Corrigé par un `useRef` qui verrouille le déclenchement dès le premier montage, indépendamment du re-render (import-wizard.tsx). Séparément : `client.ts`/L0 n'expose aucun seuil pour intercepter le `fetch` serveur vers OpenRouter depuis Playwright (`page.route` n'intercepte que le réseau du navigateur) — `e2e/import-wizard.spec.ts` (non `@canary`) frappe donc le vrai modèle, observé jusqu'à 4,4 min avec `moonshotai/kimi-k2.6` (modèle de raisonnement) sur le fixture réel. Le test passe (vérifié avec le vrai modèle ET avec un modèle invalide forçant le secours P6, les deux chemins couverts), mais `check:full` héritera de cette latence tant qu'aucun seuil de mock serveur n'existe. | Bug de concurrence réel trouvé en testant dans le navigateur (pas seulement en tests unitaires/mockés) ; latence signalée plutôt que masquée par un timeout silencieusement généreux. | Construire un seuil de mock HTTP serveur pour Playwright maintenant (infra hors scope du bloc, bénéficierait à d'autres écrans LLM futurs — à faire quand un test doit rester rapide en CI) ; changer `LLM_MODEL_SECTIONNEMENT` par défaut (choix de modèle réservé à l'humain, `llm/models.ts`) |
| 2026-07-08 | Bloc 5.1 — P10/L3, verdict | Le verdict (`acquis`/`insuffisant`) est calculé en code depuis le diff (`computeVerdict`, `src/core/correction/presentCorrection.ts`) plutôt que retourné par le modèle — L3 ne renvoie qu'un diff par point + erreurs candidates, pas de champ verdict. Règle : insuffisant si un point **critique** est `manquant` OU `deforme` (une définition déformée est au moins aussi grave qu'une définition absente — corrigé en récitation, la première formulation ne couvrait que `manquant`). Déviation du contrat ARCHITECTURE §9 ligne 3+4 / FUNCTIONS L3, qui prévoyaient un « verdict proposé » en sortie de l'appel LLM. | Même doctrine que L1 (bornes résolues en code) et L2 (couverture des gras vérifiée en code) : une règle de seuil exactement spécifiable ne doit pas dépendre du jugement libre du modèle — élimine une classe de verdicts incohérents avec le diff qu'il a lui-même produit. | Laisser le modèle renvoyer un `verdict_propose` en plus du diff (fidèle à la lettre du contrat, mais ouvre la porte à une incohérence diff/verdict que rien ne détecterait) |
| 2026-07-08 | Bloc 5.1 — L3, référencement par index | `point_index` (diff) et `recidive_index` (erreurs candidates) référencent le rang 1-based tel que numéroté dans le prompt (`rubrique_points`, `erreurs_actives`), jamais un id brut. Deux validations post-Zod dans la boucle de retry (`validateCorrectionOutput`) : bornes respectées, ET le diff couvre TOUS les points de la rubrique exactement une fois (un point omis n'est jamais silencieusement traité comme couvert). | Même précédent que L1/bornes et L2/`segments_couverts` (DECISIONS.md bloc 3.2) : un LLM reproduit mal une chaîne exacte (id UUID) mais compte correctement un rang numéroté ; la couverture complète du diff est vérifiable par le code, pas par confiance dans l'exhaustivité du modèle. | Faire correspondre les points par texte (intitulé) ; faire confiance au modèle pour ne pas omettre de point sans vérification explicite |
| 2026-07-08 | Bloc 5.1 — P10, fuite par l'explication ET les erreurs candidates | En divulgation contrôlée, P10 masque non seulement `attendu` (déjà prévu) mais aussi `explication` (le texte du LLM justifiant un point manquant/déformé peut lui-même révéler la réponse) et la `description` des erreurs candidates non couvertes (USER_FLOW É3.2 : « les intitulés d'erreurs suivent la même divulgation contrôlée que le diff ») — seuls `type` et `idErreurExistante` (badge récidive) restent visibles pour une candidate masquée. Extension découverte en récitation, au-delà du périmètre initialement décrit pour P10. Canary dédié (`presentCorrection.canary.test.ts`) sur l'objet sérialisé. | Une divulgation contrôlée qui masque le contenu attendu mais laisse fuiter la même information par un autre champ ne protège rien — signalé avant de coder plutôt que découvert après coup. | Ne masquer que `attendu` (lecture littérale mais insuffisante du contrat) |
| 2026-07-08 | Bloc 5.1 — S4.resolveOutcome, RefileItem | `resolveOutcome` (branches `retenter`/`reveler`) insère directement dans la table `RefileItem` plutôt que d'appeler `S5.refile` (qui n'existe pas encore, Phase 6). La donnée précède l'écran — même précédent que la note du PLAN Bloc 5.2 sur « retenter plus tard ». | S5/PlannerService n'existe pas avant la Phase 6 ; bloquer Bloc 5.1 en attendant reporterait tout le cycle d'étude, cœur du produit (PLAN Phase 5). | Différer `resolveOutcome` jusqu'à la Phase 6 ; construire un S5 minimal maintenant (hors scope, prématuré) |
| 2026-07-08 | Bloc 5.1 — S4.terminerSessionTemporaire | Raccourci temporaire : ferme le `StudyCycle` sur verdict final `acquis` (`closed_at` + `etat: clos`) SANS transitionner `section.statut` (reste `prete`) ni créer de `ReviewCard`. Sans lui, l'invariant « une seule session ouverte » interdirait d'étudier plus d'une section pendant toute la Phase 5, faute de `S4.validateSection`/`S6.createCard` (Phase 6, PLAN Bloc 6.2 : « validation sans Feynman autorisée temporairement pour toutes les importances, restriction rétablie en Phase 7 » — même doctrine de restriction temporaire assumée, appliquée un cran plus tôt). **TODO Phase 6** : remplacer `terminerSessionTemporaire` par `S4.validateSection` (branchée sur `S6.createCard`) ; supprimer la fonction temporaire une fois le remplacement câblé. | Signalé et tranché avec l'humain en récitation : un problème bloquant non anticipé dans le premier découpage de fichiers du bloc (l'invariant de session unique combiné à l'absence de Phase 6 rendait le cycle d'étude injouable au-delà d'une section). | Construire `S6.createCard`/`ReviewCard` maintenant (hors scope Phase 5, prématuré) ; laisser le cycle definitivement bloqué après la première section acquise (inutilisable) |
| 2026-07-08 | Bloc 5.2 — canaris promis non construits | Les deux canaris récités pour ce bloc (« insuffisant ⇒ rien de sensible dans la réponse réseau » et « U15 ne reçoit jamais le contenu du chapitre ») ne sont PAS construits en Playwright HTTP-level comme prévu : le seuil de mock du `fetch` serveur (L0/`client.ts`) vers OpenRouter n'existe toujours pas (limitation déjà notée DECISIONS.md bloc 3.3, jamais construite depuis), et Playwright `page.route` n'intercepte que le réseau du navigateur, pas le `fetch` du process `next dev`. Couverture retenue à la place : `presentCorrection.canary.test.ts` (Bloc 5.1) prouve déjà que l'objet renvoyé au client par `getCurrentCorrection`/`submitBlurting`/`resolveOutcome` (strictement la sortie de `presentCorrection`, rien ajouté par-dessus) ne contient jamais les champs masqués ; la garantie « U15 sans contenu de cours » est structurelle (type de `BlurtingEditor` sans champ contenu, vérifié par `tsc --noEmit`) + projection Drizzle `columns: { titre: true }` dans la page. Tranché avec l'humain (AskUserQuestion). | Construire le seuil de mock LLM serveur (infra cross-cutting hors scope UI, bénéficierait à tous les écrans LLM futurs — à faire quand un test doit vraiment rester rapide/déterministe en CI) et écrire des specs Playwright contre le vrai modèle (non `@canary`, lents/payants) ont tous deux été proposés et écartés par l'humain pour ce bloc. | Construire le seuil de mock serveur maintenant ; écrire des specs Playwright non-canary contre le vrai modèle |
| 2026-07-09 | Bloc 5.3 — S7.commitCandidates, compteur de récidive | `ErrorEntry` (ARCHITECTURE §8 v0.4) n'avait aucune colonne pour « le compteur de l'erreur active » (FUNCTIONS §3 S7) ni le « compteur de récidives » (USER_FLOW É5.1). Ajout de `occurrences INTEGER NOT NULL DEFAULT 1` (migration `0002_error_entry_occurrences.sql`) : `commitCandidates` incrémente cette colonne quand `idErreurExistante` est non-nul, insère une nouvelle ligne (`occurrences=1`) sinon — jamais de doublon. ARCHITECTURE.md v0.5. | Tranché avec l'humain (AskUserQuestion) — recommandation retenue : un compteur stocké est trivial à incrémenter/lire, contre un recalcul dynamique par scan des JSONB `StudySession.correction` de toutes les sessions de la section (fragile — dépend de la forme interne du JSON — et coûteux à chaque affichage du carnet). | Recalcul dynamique par scan des `StudySession.correction` (pas de migration, mais fragile et coûteux) |
| 2026-07-09 | Bloc 5.3 — S7.commitCandidates, édition d'une candidate récidive | Une candidate avec `idErreurExistante` non-nul (récidive détectée par L3) n'est **pas éditable** dans `ErrorCandidatesPanel` avant commit — seulement accepter/rejeter ; le texte affiché (si divulgation complète) reste celui du LLM, non modifiable à cet endroit. Éditer la description de l'erreur déjà existante reste le rôle de `S7.edit`, exposé dans le carnet (U24), un point d'entrée distinct. | Tranché avec l'humain (AskUserQuestion) — recommandation retenue : mélanger l'écriture de `commitCandidates` (créer/incrémenter) et celle d'`edit` (corriger une entrée existante) pour la même ligne aurait dupliqué la même mutation par deux chemins différents. | Autoriser l'édition du texte d'une récidive au moment du commit (aurait fait de `commitCandidates` un second point d'entrée pour la même mutation que `S7.edit`) |
| 2026-07-09 | Bloc 5.3 — U24, lien « voir la session d'origine » | Nouvelle route `/session/[id]` (lecture seule) construite ce bloc : réutilise l'affichage diff + candidates (divulgation complète, la session est close et immuable) + badge « override » si `AuditService` a un événement `revelation_correction` rattaché à cette session. Route absente du catalogue U1–U24 explicite, mais exigée telle quelle par USER_FLOW É5.1. | Tranché avec l'humain (AskUserQuestion) — recommandation retenue : USER_FLOW exige la destination pour ce bloc précis (pas un lien mort), et les pièces d'affichage (diff, candidates) existent déjà à réutiliser (AGENTS §2, gains de réutilisation FUNCTIONS §6.3). | Stub désactivé, destination réelle reportée à un bloc non planifié explicitement |
| 2026-07-09 | Bloc 5.3 — revue ponytail-review, U17 édition pré-commit retirée | La possibilité d'éditer le texte d'une candidate SANS récidive avant commit (ajoutée au-delà de ce qui avait été explicitement tranché — seule la non-éditabilité d'une récidive avait été validée par l'humain) est retirée : `CandidateDecision.description`/`editedDescriptions`, `encodeDecisions`/`parseCandidateDecisions`, et les paramètres `editedCandidateDescriptions` de `S4.resolveOutcome`/`terminerSessionTemporaire` sont supprimés. `ErrorCandidatesPanel` ne garde plus qu'accepter/rejeter (`rejected: boolean[]`). | Revue `/ponytail-review` : ~55 lignes dupliquant une mutation (corriger le texte d'une ErrorEntry) déjà couverte par `S7.edit` dans le carnet — éditer après commit a le même effet, avec un seul point d'entrée pour cette mutation au lieu de deux. `ErrorEntryNotFoundError` (classe custom jamais interceptée par nom) remplacée par un `Error` générique, même logique. | Garder l'édition pré-commit malgré la duplication constatée |
| 2026-07-09 | Session — un bloc par session (CLAUDE.md §1) | La même session enchaîne sur le Bloc 6.1 juste après avoir livré et commité le Bloc 5.3 (+ sa revue ponytail-review), en dérogation à « une session = un bloc ». Le critère de sortie de Phase 5 (« tu as étudié ≥ 3 sections réelles via l'app ») n'est pas non plus vérifié — c'est un usage réel que je ne peux pas simuler à la place de l'humain. | Signalé explicitement avant de continuer (AskUserQuestion) ; l'humain a choisi de déroger à la règle en connaissance de cause plutôt que d'ouvrir une nouvelle session ou de faire autre chose d'abord. | Nouvelle session pour le Bloc 6.1 ; s'arrêter et attendre l'usage réel de Phase 5 |
| 2026-07-09 | Bloc 6.1 — P7 buildDailyQueue, tri « proximité d'examen » | Une matière dont la date d'examen est déjà passée est traitée comme « pas d'examen » (distance infinie) dans le tri des révisions dues et des nouvelles études — pas de priorité artificielle une fois l'examen passé, le tri retombe sur (retard/importance) puis (importance/ordre curriculum). Comportement fixé AVANT d'écrire le `@canary` correspondant (« examen passé », PLAN Phase 6) qui le gèlera. | Tranché avec l'humain (AskUserQuestion) — recommandation retenue : ARCHITECTURE §3 ne spécifie pas ce cas ; une urgence artificielle sur un examen déjà passé n'aurait aucun sens pédagogique. | Distance négative ⇒ priorité maximale (rattraper une matière dont l'examen vient de passer) |
| 2026-07-09 | Bloc 6.2 — P9 fsrsCore + S6 ReviewService, récitation | (1) `ReviewCard` (ARCHITECTURE §8 v0.5) n'avait aucun champ pour l'état gelé alors que FUNCTIONS §7 fait de S6 « le seul propriétaire du gel » (3 appelants) — ajout de `gelee BOOLEAN DEFAULT false` (migration `0003_real_lord_hawal.sql`, ARCHITECTURE v0.6). `due` n'est jamais retouché au dégel : ts-fsrs recalcule déjà depuis les dates réelles au prochain `rate` (pas de recalcul manuel nécessaire). (2) P9 n'ajoute aucun autre champ : `enable_short_term: false` (ts-fsrs) confirmé par expérimentation directe — une carte reste toujours New (reps=0) ou Review (reps>0), jamais Learning/Relearning, donc `state`/`learning_steps` ts-fsrs n'ont pas besoin d'être persistés, les 6 colonnes existantes suffisent. (3) `S6.createCard` ne touche jamais `section.statut` — la transition `validee → en_revision` (Machine A) reste possédée par `S4.validateSection`, qui l'appelle, même précédent que S3 gérant `rubrique_a_valider → prete`. (4) `terminerSessionTemporaire` (DECISIONS.md bloc 5.1) supprimée, remplacée par `S4.validateSection` branchée sur `S6.createCard`, comme prévu par le TODO daté. | Tranché avec l'humain (AskUserQuestion) pour (1) — recommandation retenue : un booléen possédé par S6 évite de recréer l'ambiguïté de propriétaire critiquée en FUNCTIONS §0 (dériver le gel de `section.statut` ailleurs, au Bloc 6.3, aurait dilué l'invariant). (2)-(4) tranchés en récitation avant code. | (1) Dériver le gel de `section.statut` sans nouvelle colonne (rejeté, cf. ci-dessus). |
| 2026-07-09 | Session — bloc 6.3 enchaîné sans commit du bloc 6.2 | À la demande explicite de l'humain (« go prochain bloc sans commit »), le Bloc 6.3 démarre alors que le Bloc 6.2 n'est pas encore commité — dérogation à « un commit par bloc » (AGENTS §4), même doctrine que DECISIONS.md #44 (dérogation actée sur demande explicite plutôt que devinée). | Instruction directe de l'humain. | Committer le Bloc 6.2 d'abord (non demandé). |
| 2026-07-09 | Bloc 6.3 — S5 PlannerService, récitation | (1) `QueueOrder(date PK, order JSONB)` ajoutée (ARCHITECTURE v0.7, migration `0004_complex_war_machine.sql`) — FUNCTIONS §7 fait de S5 le seul propriétaire d'une permutation manuelle datée, absente d'ARCHITECTURE §8 v0.6. Réconciliation en lecture : items de l'ordre stocké encore présents gardent leur position, tout nouvel item (ex. re-file arrivé en cours de journée) s'ajoute en fin, dans l'ordre naturel de P7. (2) Un item `kind: revision` peut apparaître dans la file dès ce bloc (ReviewCard créées depuis le Bloc 6.2) alors que l'écran de révision (USER_FLOW P4) est Bloc 6.4 : la carte s'affiche normalement (dette visible, ARCHITECTURE §3) mais son `[Commencer]` est désactivé avec un libellé « Bloc 6.4 ». (3) `S4.resolveOutcome` appelait `RefileItem` en direct (DECISIONS.md #37, « propriété migrera vers S5.refile ») — remplacé par `planner.refile`, honorant ce TODO. (4) `ordreCurriculum` (tiebreaker P7) n'a pas de champ dédié en base (aucun `chapter.ordre`) : approximé par tri stable (subject.ordre, chapter.maj, section.ordre) — dernier rang après importance/examen, impact produit négligeable. | (1)-(2) tranchés avec l'humain (AskUserQuestion) avant code — recommandations retenues. (3)-(4) tranchés en récitation, jugement d'implémentation sans impact produit. | (1) Dériver l'ordre de `section.statut`/`RefileItem` sans nouvelle table (rejeté, aurait recréé la même ambiguïté que le gel au Bloc 6.2). (2) Exclure les cartes de révision de la file ce bloc-ci (rejeté : masquerait une dette réelle, contraire à l'esprit du Planificateur). |
| 2026-07-09 | Bloc 6.4 — Révision + S3.lazyScheduler + tâches de fond, récitation | (1) Cron : **Vercel Cron** retenu (pas de pg_cron) — `app/api/cron/route.ts` protégée par `CRON_SECRET` (`Authorization: Bearer`), `vercel.json` (`0 3 * * *`). Le déclenchement réel (FUNCTIONS §5 : lazyScheduler = « tâche de fond + hook de S5 ») est double : hook non bloquant via `after()` dans `app/(app)/page.tsx` juste après `planner.todayQueue` (couvre les visites de l'accueil), et le cron rattrape les jours sans visite + purge (`S5.purgeExpired` : RefileItem/QueueOrder antérieurs à aujourd'hui). `after()` n'est PAS appelé depuis `planner.ts`/`guide.ts` eux-mêmes : Next lève une erreur (« called outside a request scope ») hors contexte de requête, ce qui aurait cassé tous les tests Vitest de S3/S5 appelant ces fonctions directement — le hook vit donc dans la page, pas dans le service. (2) Fenêtre de `S3.lazyScheduler` : `config.nouvellesParJour` (même plafond que les nouvelles études du jour), sections `active` triées comme S5 (importance DESC, examen, ordre curriculum), **importance 2 totalement exclue** du tri (jamais pré-générée, seul le bouton manuel `generate`/`generateBatch` y donne accès). (3) `/revision/[cardId]/` : `cardId` = `section.id` (ReviewCard unique par section, P7 ne connaît que sectionId, aucune jointure supplémentaire dans `DailyQueue`). (4) `S4.start` généralisé : le type de cycle (étude/révision) se déduit du statut de la section (`prete`/`en_revision`) plutôt qu'une fonction `startRevision` séparée — FUNCTIONS §3 ne liste qu'un seul point d'entrée `start`. (5) `S4.validateSection` : une section repassée `prete` après 2×Again peut se re-valider alors qu'une `ReviewCard` existe déjà (contrainte d'unicité) — `ReviewCardAlreadyExistsError` est désormais ignorée silencieusement (carte existante conservée, historique FSRS — stability/difficulty/lapses — jamais réinitialisé) plutôt que de faire échouer `validateSection`. | (1)-(3) tranchés avec l'humain (AskUserQuestion) avant code — recommandations retenues. (4)-(5) tranchés en récitation (lecture directe de FUNCTIONS §3, pas d'entrée séparée listée) puis découverte technique en cours d'implémentation pour le placement d'`after()`. | (1) pg_cron (rejeté : infra Postgres supplémentaire pour un projet mono-utilisateur non encore déployé). `after()` directement dans `S3.lazyScheduler`/`S5.todayQueue` (rejeté : casse les tests Vitest existants, confirmé en lisant `next/dist/server/after/after.js`). (5) Réinitialiser la ReviewCard à la re-validation (rejeté : perd l'historique des lapses/difficulty sans qu'aucun document ne le demande). |
| 2026-07-09 | Bloc 7.0 — SPIKE transcription, décision GO | **GO** sur un modèle audio via OpenRouter (ADR 9 vigilance levée, fallback Web Speech API écarté). Prototype jetable (`scripts/spike-transcription.mjs`, supprimé après le spike) : `chat/completions` avec content-type `input_audio` (base64, pas d'URL directe — confirmé par la doc OpenRouter) fonctionne. Modèle retenu : `google/gemini-2.5-flash` — testé sur un extrait audio réel (toi expliquant l'article 1119 du Code civil sur les conditions générales), transcript fidèle, vocabulaire juridique technique préservé exactement (« article 1119 », « instrumentum », « stipulations non négociables »), rien d'inventé. `openai/gpt-4o-audio-preview` testé en parallèle : ID de modèle invalide sur OpenRouter, non réessayé (un seul candidat GO suffisant, tranché avec l'humain plutôt que d'explorer d'autres modèles). `LLM_MODEL_TRANSCRIPTION=google/gemini-2.5-flash` ajouté à `.env.local`/`.env.example`. | Tranché avec l'humain (AskUserQuestion) : résultat du spike jugé suffisant pour clore le bloc sans tester d'autre modèle. | Web Speech API (fallback prévu, écarté — le modèle OpenRouter fonctionne dès le premier essai) ; comparer d'autres modèles audio avant de trancher (écarté, non nécessaire vu la qualité du résultat) |
| 2026-07-09 | Session — bloc 7.1 enchaîné sans commit du bloc 7.0 | À la demande explicite de l'humain, le Bloc 7.1 démarre alors que le Bloc 7.0 n'est pas encore committé — dérogation à « un commit par bloc » (AGENTS §4), même doctrine que DECISIONS.md #47. | Instruction directe de l'humain (AskUserQuestion, option « enchainer sans committer »). | Committer le Bloc 7.0 d'abord (proposé, écarté par l'humain). |
| 2026-07-09 | Bloc 7.1 — L6 + U20, récitation + vérif format audio reportée | (1) `S4.transcribe` ajouté comme point d'entrée **distinct** de `S4.feynman` (qui n'existe pas avant le Bloc 7.2) — colle aux deux étapes séparées d'USER_FLOW É3.3 (transcrire ⇒ éditer ⇒ envoyer), pas de garde sur un état dédié du cycle (cette transition machine B n'existe pas encore). (2) `client.ts` : nouvelle fonction `callTranscription` distincte de `callLLM` (sortie texte libre vs JSON+Zod, content multimodal `input_audio` vs texte) — partie HTTP commune extraite dans `postChatCompletion` plutôt que dupliquée. (3) U20 (`push-to-talk-recorder.tsx`) choisit explicitement un `mimeType` supporté via `MediaRecorder.isTypeSupported` (jamais un guess) parmi mp4/webm/ogg — mais **la vérification réelle en navigateur (mimeType effectif → accepté par `google/gemini-2.5-flash` sur OpenRouter) est reportée au Bloc 7.2** : U20 n'est composé dans aucun écran réel ce bloc-ci (U19/FeynmanChat = Bloc 7.2), donc rien n'est cliquable pour tester ; construire une route de test jetable maintenant a été proposé et écarté (hors plan de fichiers validé en récitation). (4) Pas de test unitaire (Vitest/RTL) pour U20 : le projet n'a ni `jsdom` ni Testing Library (absents de TECH_MAPPING, `MediaRecorder`/`getUserMedia` sont des API navigateur) — vérification structurelle (tsc + eslint) + Playwright au Bloc 7.2, même doctrine que DECISIONS.md #39. | (1)-(2) tranchés en récitation avant code (lecture directe de FUNCTIONS §3/USER_FLOW É3.3). (3) Tranché avec l'humain (AskUserQuestion) après coup, une fois le manque d'écran réel constaté en fin de bloc. (4) Constat d'infra, pas un arbitrage produit. | (3) Construire une page de test jetable hors PLAN pour lever le risque maintenant (proposé, écarté par l'humain — reporté à 7.2). |
| 2026-07-09 | Session — bloc 7.2 enchaîné sans commit (7.0+7.1) et sans repasser par une validation de récitation | À la demande explicite de l'humain (« fais le bloc 7.2 sans commit sans relancer »), le Bloc 7.2 démarre sans committer 7.0/7.1 (même doctrine que #47/#51) ET sans attendre de validation humaine de la récitation avant de coder (AskUserQuestion réservé aux points réellement ambigus/produit de ce bloc, pas au canari de compréhension lui-même). | Instruction directe de l'humain. | S'arrêter pour validation de récitation comme les blocs précédents (écarté explicitement). |
| 2026-07-09 | Bloc 7.2 — L4 + L5 + prompts + U19 + U21, récitation en cours de code | (1) **Deux fichiers de prompt** (`feynman_turn.v1.md`, `feynman_bilan.v1.md`) plutôt qu'un seul `feynman.v1.md` comme le nom laissait entendre en PLAN — L4 (texte libre streamé) et L5 (JSON structuré) ont des contrats trop différents ; les deux partagent le même `appel` PromptLog/modèle (`feynman`, un seul `LLM_MODEL_FEYNMAN`) via un nouveau paramètre `promptFile` sur `callLLM`/`streamCompletion` (L0). (2) `client.ts` : nouvelle fonction `streamCompletion` (SSE `stream:true` + `stream_options.include_usage`, parsing manuel `data: {...}`) — **pas de retry avec réinjection** pour un flux déjà partiellement affiché à l'utilisateur (contrairement à `callLLM`/`callTranscription`) : un échec se propage tel quel, l'UI porte son propre `[Réessayer]`. (3) `StudyCycle.bilan_feynman JSONB` ajouté (migration `0005_lowly_namor.sql`, ARCHITECTURE v0.8) : le bilan est un artefact UNIQUE par cycle, pas une tentative (`study_session.type` n'a pas de valeur "bilan") — canari `schema.canary.test.ts` mis à jour en conséquence (colonne ajoutée, pas une assertion assouplie). (4) Architecture streaming : **Route Handler** (`app/api/feynman/turn/route.ts`), pas un Server Action — Next 16 documente le modèle Server Action comme *single-roundtrip response*, incompatible avec un flux token-par-token (vérifié dans `node_modules/next/dist/docs`, conforme à la consigne AGENTS.md en tête de fichier). `S4.beginFeynman`/`S4.beginRestartFeynman` (transition seule, redirigeants, appelés en Server Action classique) sont **scindés** de `S4.openingTurn`/`S4.feynmanTurn` (génération streamée, appelés depuis le Route Handler) — un seul `openingTurn` sert à la fois la toute première ouverture (historique vide) ET la reprise après [Refaire un Feynman] (historique réel préservé, ADR 6) : pas de fonction dupliquée. (5) Restriction « Feynman requis si importance ≥ 3 » rétablie dans `validateSection` (bypass temporaire du Bloc 6.2 levé) — **deux fixtures de test existantes cassées** (`session.test.ts` `createReadySection` importance 3→2 par défaut ; `session.canary.test.ts` `createRevisionSection` idem) : changement de données de fixture uniquement, aucune assertion modifiée ni canari déplacé/renommé (AGENTS §2.5 respecté à la lettre). (6) Pas de test unitaire U19/U21 (mêmes raisons d'infra que U20, DECISIONS.md #52) ; vérification : `tsc` + `eslint` + `next build` + smoke `/login` 200 — le vrai dialogue Feynman à voix haute reste à tester par l'humain (impossible à simuler ici, même limite que Bloc 5/6). | Tranché en récitation avant/pendant le code (lecture directe de FUNCTIONS §2/§3, USER_FLOW É3.3-É3.4, ARCHITECTURE §8/§9, et des docs Next 16 pour (4) — AGENTS §2.5 pour (5)). Aucun point bloquant nécessitant l'humain identifié pendant l'implémentation (« sans relancer », demande explicite). | (4) Server Action seule avec un stub de streaming côté client (rejeté : violerait le modèle documenté, aurait forcé un faux streaming). Dupliquer `openingTurn` en `restartOpeningTurn` (rejeté après coup : `loadFeynmanTours` gère déjà les deux cas naturellement). |
| 2026-07-09 | Bloc 8.1 — P4 matchSections, récitation | (1) Contrat de « nouvel arbre » (FUNCTIONS §1) : `matchSections` reçoit `titleTree` (P1) + le markdown complet, et aplatit lui-même l'arbre via `selectCandidateHeadings`/`mechanicalSectioning` (P6) — même cascade Titre2→Titre1→chapitre entier que S2.propose à l'import initial, symétrie avec la façon dont les sections existent déjà. Pas de pré-extraction côté appelant (Bloc 8.2). (2) Trois passes dans l'ordre littéral de FUNCTIONS §1 : hash de contenu exact (`computeContentHash`, P3, réutilisé tel quel — position-indépendant, gère aussi les doublons de contenu par appariement de groupe trié par position) ; titre exact (idem, groupé) ; similarité titre+position (Jaccard sur les mots du titre + proximité d'ordre normalisée, seuil 0.5, aucun candidat si zéro mot commun) en dernier recours. Un groupe ambigu (comptes différents, aucun score au-dessus du seuil) n'est jamais forcé — reste `disparue`/`nouvelle` (seuil conservateur explicite de FUNCTIONS §1). (3) Fixtures : aucune vraie paire avant/après d'un ré-import n'existe encore dans `e2e/evals/fixtures/` (2 chapitres figés seulement) — les scénarios (renommage, réordonnancement, fusion, scission implicite) sont construits, mais le texte juridique repris est authentique (extraits réels d'« Introduction obli.md »), pas inventé. | (1)-(2)-(3) tranchés avec l'humain (AskUserQuestion) en récitation, avant code — recommandations retenues. | (1) Le caller (S1, Bloc 8.2) pré-extrait les blocs du nouvel arbre (rejeté : aurait reporté une logique d'extraction non testée par P4 lui-même en dehors de sa propre suite). (3) Attendre un vrai ré-import réel avant d'écrire la suite exhaustive (rejeté : bloquerait le bloc indéfiniment, aucune date connue pour la prochaine vraie modification de cours). |
| 2026-07-09 | Session — bloc 8.2 enchaîné sans nouvelle session après le commit du bloc 8.1 | À la demande explicite de l'humain (« passe au bloc 8.2 sans faire une nouvelle session »), dérogation à « une session = un bloc » (CLAUDE.md §1), même doctrine que DECISIONS.md #44/#47/#51/#53 — mais ici le Bloc 8.1 est committé au préalable (pas de dérogation cumulée au commit par bloc). | Instruction directe de l'humain. | Nouvelle session pour le Bloc 8.2 (non demandé). |
| 2026-07-09 | Bloc 8.2 — P5 diffChapterVersions + S1.simulateUpdate/commitUpdate + U6/U7, récitation | (1) Badge « cours modifié » (S5.attentionBadges) **dérivé**, pas de nouvelle colonne : `correctionGuide.statut='obsolete' AND correctionGuide.chapterVersion < section.chapterVersion` — `commitUpdate` obsolète une rubrique SANS toucher son `chapterVersion` propre (reste ancrée à l'ancienne version), alors que la Section, elle, est bumpée à la nouvelle ; distingue sans ambiguïté une invalidation par cascade d'un `S3.regenerate` manuel (qui recrée aussitôt une rubrique non-obsolète, même version). Testé dans les deux sens (`planner.test.ts`). (2) Sections « intactes » (hash identique) : `ordre` et `chapterVersion` mis à jour par `commitUpdate`, tout le reste (contenu/titre/segments/statut/rubrique/ReviewCard) laissé à la lettre — sinon l'ordre curriculum dérive silencieusement à chaque ré-import ultérieur (tiebreaker P7). L'ordre final (appariements + nouvelles) est recalculé globalement par tri sur la position dans le nouveau markdown : les 3 passes de P4 ne garantissent un ordre que dans leur propre groupe, pas sur l'ensemble. (3) P5 (`diffChapterVersions`) délègue l'alignement de séquence à `Diff.diffArrays` avec un comparateur sur le titre (LCS fourni par jsdiff, pas réimplémenté) puis `Diff.diffWords` à l'intérieur de chaque paire — TECH_MAPPING §6 : jsdiff pour le diff textuel, alignement par titres maison au-dessus, rien de plus. `diff`/`@types/diff` ajoutés en dépendance directe (`package.json`) : présents seulement en transitif jusqu'ici, déjà pré-approuvés par TECH_MAPPING, pas une lib hors-liste. (4) `simulateUpdate`/`commitUpdate` recalculent tout eux-mêmes côté serveur à chaque appel plutôt que de faire confiance à un payload de simulation venant du client — même doctrine que `importChapter` avec les anomalies. Gel des ReviewCards (S6.freeze) appelé après la transaction principale, en séquence non transactionnelle — même façon de composer que `S4.validateSection` appelant `S6.createCard` (precedent déjà établi, pas de variante tx-aware dans S6). (5) `extractSection`/`assertChapterOwnership`, privés dans `section.ts` (S2), exportés et réutilisés par `chapter.ts` (S1) plutôt que dupliqués une troisième fois. (6) U6/U7 construits comme composants autonomes, non composés dans un écran réel (É6.2 attend U4/Tiptap — Bloc 8.3 ; É6.3 est son propre bloc — 8.4) : rien à tester au navigateur pour ce bloc, comme pour le 8.1. Surlignage « sera écrasé » (U6, retouches locales non reportées) non câblé : aucun mécanisme n'écrit encore `chapter.markdown` hors de l'import initial avant l'éditeur intégré — reporté au Bloc 8.3. | (1)-(2) tranchés avec l'humain (AskUserQuestion) en récitation, avant code — recommandations retenues. (3)-(6) tranchés en récitation (lecture directe de FUNCTIONS §1/§3, ARCHITECTURE §7, TECH_MAPPING §6, USER_FLOW É6.1-É6.3). | (1) Nouvelle colonne dédiée `section.cours_modifie` (rejeté : migration + 3ᵉ point de mise à jour pour un signal déjà dérivable). (2) Ne rien toucher aux sections intactes, même l'ordre (rejeté : dérive silencieuse de l'ordre curriculum au fil des ré-imports). |
| 2026-07-09 | Session — bloc 8.3 enchaîné sans nouvelle session après le commit du bloc 8.2 | À la demande explicite de l'humain, dérogation à « une session = un bloc » (CLAUDE.md §1), même doctrine que #44/#47/#51/#53/(bloc 8.2). Bloc 8.2 committé au préalable. | Instruction directe de l'humain. | Nouvelle session pour le Bloc 8.3 (non demandé). |
| 2026-07-09 | Bloc 8.3 — U4 CourseEditor/Tiptap, É6.2, récitation | (1) **Route manquante** : aucune page É6.1 « Vue chapitre » n'existait (`curriculum-ui.tsx` listait les chapitres en texte brut). Créé `/curriculum/chapitre/[id]` — vue minimale (U3) + `[Modifier dans l'app]` → U4, strictement scopé à 8.3 : pas de Re-trier/Générer en lot/Archiver/Supprimer/Ré-importer (blocs futurs ou déjà ailleurs). (2) **Schéma Heading Tiptap 1–6, barre d'outils 1–3** : les deux fixtures réelles montent jusqu'à Titre 5 (FORMAT §1) ; un schéma strict 1–3 aurait perdu ces niveaux au premier chargement dans l'éditeur, contredisant directement l'exigence de round-trip (TECH_MAPPING §5 point 4). La contrainte « impossible à saisir » porte sur la SAISIE (3 boutons H1/H2/H3 seulement dans la barre d'outils), pas sur la fidélité d'un document déjà importé. Gras+italique combinés restent saisissables au clavier (ProseMirror n'expose pas d'exclusion de marks via `StarterKit.configure()` sans étendre l'extension) — filet de sécurité identique à l'import : `grasItaliqueAmbigu` (P2, déjà réutilisé pour la validation continue de hiérarchie) détecte et bloque la sauvegarde tant que l'anomalie n'est pas acquittée, même mécanisme que pour un import Google Docs. (3) **Retrofit S1** : `simulateUpdate`/`commitUpdate` (Bloc 8.2) n'avaient aucun garde-fou anomalies alors qu'USER_FLOW É6.2 ET É6.3 exigent toutes deux « mêmes règles qu'É1.1–É1.2, rapport de validation compris ». `commitUpdate` accepte désormais `acknowledgedAnomalyKeys` et lève `AnomaliesNonAcquitteesError` (même classe, message généralisé) si non couvertes ; `simulateUpdate` remonte `anomalies: Anomaly[]` sans bloquer (l'écran affiche AnomalyPanel avant le dialogue de conséquences). Testé (`chapter.test.ts`). (4) **Sérialisation Markdown↔Tiptap 100% maison** (`course-editor-markdown.ts`) : mdast (remark-parse, déjà installé) → JSON Tiptap et l'inverse via remark-stringify — TECH_MAPPING §5 ne nomme aucune lib de sérialisation, seulement « Tiptap + sérialisation Markdown » ; couche fine de projection, même doctrine que P1/P3. Fonctions PURES, testées et canari-isées AVANT toute installation de `@tiptap/*` (conforme à PLAN.md Bloc 8.3). Support ajouté pour les **liens** (mark `link`), découverts par le canari lui-même dans les deux fixtures réelles (citations Légifrance/Conseil constitutionnel) — non documentés par FORMAT.md, ajoutés pour ne pas perdre du contenu réel. Blockquote/table/image (tolérés par FORMAT §2.6 mais absents des fixtures) volontairement non supportés — un nœud non reconnu lève une erreur explicite. (5) **Invariant de round-trip reformulé** : le canari initial (hash strictement identique à l'original) échouait sur les deux fixtures à cause des listes « spread » (item·s séparés par une ligne vide) — une distinction purement syntaxique que mdast ne fait porter que sur le rendu HTML des paragraphes internes (jamais sur le texte), et qu'aucune extension Tiptap/ProseMirror ne modélise de toute façon (confirmé : n'importe quel éditeur Tiptap réel aplatirait pareil dès le premier chargement). Remplacé par deux invariants plus justes, tous deux verts sur les fixtures réelles : stabilité après la 1ʳᵉ réécriture (point fixe, hash) + aucun mot du cours perdu (comparaison texte-à-texte, `course-editor-markdown.canary.test.ts`). | (1)-(3) tranchés avec l'humain (AskUserQuestion) en récitation, avant code — recommandations retenues. (4)-(5) découverts et tranchés en cours de code (le canari de round-trip lui-même a révélé les liens et la question spread/tight), signalés plutôt que masqués. | (2) Schéma strict 1–3 (rejeté : perte de contenu réel avérée). (3) Laisser le trou pour un bloc séparé (rejeté : risque réel côté É6.3/Bloc 8.4, ré-import de texte externe). |
| 2026-07-09 | Bloc 8.3 — test navigateur (humain), correction | « Titres/listes ne marchent pas » constaté en navigateur : le reset Tailwind dépouille `h1`–`h6`/`ul`/`ol` de tout style par défaut — les commandes Tiptap basculaient bien de balise (h1/ul réels, sérialisation Markdown déjà correcte) mais restaient visuellement identiques au texte courant, d'où l'illusion d'un éditeur cassé. Classes de `MarkdownViewer` (U3, `headingClass`/`list-disc`) réappliquées à `course-editor.tsx` en sélecteurs descendants Tailwind (`[&_.tiptap_h1]:...`) faute de contrôler le rendu HTML de ProseMirror nœud par nœud. | Bug réel trouvé en testant dans le navigateur (comme DECISIONS.md #33) — corrigé avant de committer le bloc. | — |
| 2026-07-09 | Session — bloc 8.4 enchaîné sans nouvelle session après le commit du bloc 8.3 | À la demande explicite de l'humain (« commit and continue »), dérogation à « une session = un bloc » (CLAUDE.md §1), même doctrine que #44/#47/#51/#53/(8.2)/(8.3). Bloc 8.3 committé au préalable, correction du test navigateur incluse. | Instruction directe de l'humain. | Nouvelle session pour le Bloc 8.4 (non demandé). |
| 2026-07-09 | Bloc 8.4 — É6.3 ré-import, récitation | (1) **Bug réel corrigé avant code** : `S5.attentionBadges.chapitresNonTries` testait « zéro section non-`a_trier` » — correct tant qu'un chapitre n'était triable qu'en un seul bloc à l'import (Bloc 6.3), faux depuis que le ré-import (Bloc 8.2) peut laisser un chapitre dans un état mixte (sections déjà triées + nouvelles `a_trier`). Remplacé par : chapitre compté dès qu'il a zéro section (jamais sectionné) OU au moins une section `a_trier` (mixte ou jamais triée) — union des deux anciens cas, un seul chargement en mémoire (`inArray`) plutôt que `selectDistinct` + `Set`. Nouveau test (`planner.test.ts`, cas mixte) ; les 2 tests existants inchangés dans leur assertion. (2) `chapter-editor-screen.tsx` (Bloc 8.3) généralisé de `editing: boolean` à `mode: "vue" \| "edition" \| "reimport"` — le mode ré-import réutilise EXACTEMENT le même pipeline `simulateUpdateAction`/`commitUpdateAction`/`AnomalyPanel`/`ConsequencesDialog` que l'édition (Blocs 8.2/8.3), seule la source du markdown change (upload/collage brut, mêmes composants qu'`import-wizard.tsx` É1.1, plutôt que `CourseEditor`). Aucun nouveau Server Action. (3) Surlignage « sera écrasé » (U6) : confirmé hors scope — nécessiterait de distinguer la source de la dernière modification (`chapter.markdown` n'a qu'un seul chemin d'écriture, `commitUpdate`, identique pour É6.2 et É6.3), décision de modèle de données non demandée explicitement par ce bloc. Le diff (U6, dans `ConsequencesDialog`) reste visible sans ce surlignage spécifique. | (1)-(2) tranchés avec l'humain (AskUserQuestion) en récitation, avant code — recommandations retenues. | (1) Laisser le badge tel quel (rejeté : resterait faux après un vrai ré-import, contredit USER_FLOW É6.3 point 4). (3) Ajouter une colonne `chapter.derniere_modification_source` maintenant (rejeté : portée au-delà de ce que USER_FLOW décrit précisément, reporté explicitement). |
| 2026-07-09 | Phase 8 — sortie confirmée | L'humain confirme avoir testé avec succès le scénario de sortie de PLAN.md Phase 8 (modifier un vrai cours dans Google Docs, ré-importer, constater qu'aucun historique n'est perdu). Phase 8 officiellement close avant d'ouvrir la Phase 9. | Confirmation explicite de l'humain (AskUserQuestion), conformément à la règle PLAN.md « on ne passe jamais à la phase suivante sans critère de sortie rempli ». | — |
| 2026-07-09 | Session — bloc 9.1 enchaîné sans nouvelle session | À la demande explicite de l'humain (« continue passe au prochain »), dérogation à « une session = un bloc » (CLAUDE.md §1), même doctrine que les précédentes cette session. | Instruction directe de l'humain. | Nouvelle session pour le Bloc 9.1 (non demandé). |
| 2026-07-13 | Bloc 9.1 — clôture, canari rouge en session | `npm run check` révélait un canari intermittent (`transcribeAudio.canary.test.ts`, 1 ligne PromptLog attendue / 3 reçues). Root cause : pool Vitest par défaut (`threads`, worker_threads) partage `process.env` entre fichiers via le proxy Node vers le thread principal — les `beforeEach` concurrents de `client.test.ts` et `transcribeAudio.canary.test.ts` qui réécrivent `LLM_MODEL_TRANSCRIPTION` se marchent dessus, des lignes PromptLog atterrissent sous le mauvais `model`. Fix : `vitest.config.ts` → `pool: "forks"` (isolation par processus OS réel). Vérifié sur 3 exécutions consécutives de la suite complète + `npm run check` : vert. Pré-existant depuis le Bloc 3.1 (L0), sans lien avec le diff du bloc 9.1 — corrigé avant de committer plutôt que de contourner. | Root cause exigée avant tout commit (AGENTS.md §2.5 : un canari rouge invalide la modification, jamais le canari) ; fix dans le harnais (vitest.config.ts), aucun fichier `*.canary.*` modifié. | Retenter le test en boucle jusqu'à ce qu'il passe (rejeté : ne corrige rien, le prochain run concurrent aurait re-flaké) ; supprimer les mutations de `process.env` dans les tests concernés au profit d'un paramètre explicite (rejeté : refactor de L0/tests hors scope du bloc 9.1, plus large que nécessaire pour ce bug précis). |
| 2026-07-13 | Bloc 9.1 — deux canaris manquants posés (PLAN.md Phase 9) | Ni `@canary` « archiver puis désarchiver ⇒ échéances/historique intacts » ni `@canary` « export JSON re-parsable complet » n'existaient encore (DoD du bloc, AGENTS.md §5 point 2). Ajoutés : `src/services/planner.canary.test.ts` (archive un chapitre ⇒ absent de `todayQueue` ; désarchive ⇒ réapparaît, `ReviewCard` et `StudySession` de la section comparés avant/après par égalité stricte) et `src/services/account.canary.test.ts` (`exportUserData` semé avec un exemplaire de chaque table journalisée + une matière archivée, `JSON.parse(JSON.stringify(...))` comparé collection par collection). Formulation PLAN.md « échéances recalculées » non reprise littéralement : le comportement réellement décidé et implémenté (Bloc 9.1, entrée du dessous) est un pur filtre de statut, sans recalcul FSRS — le canari vérifie l'invariant tel qu'arbitré avec l'humain, pas la formulation d'origine du plan. `npm run test:canary` vert (70 tests Vitest canaries + 1 Playwright). | Exigé par la DoD du bloc avant tout commit ; écart de formulation par rapport au PLAN.md signalé plutôt que résolu silencieusement (AGENTS.md §1). | Committer sans ces canaris en reportant leur ajout à un bloc ultérieur (rejeté : PLAN.md les rattache explicitement à la Phase 9, dont 9.1 est le bloc qui construit le comportement qu'ils protègent). |
| 2026-07-13 | Bloc 9.1 — test navigateur (humain), clôture | L'humain confirme avoir déroulé en navigateur : archivage/désarchivage matière et chapitre, suppression gardée (double confirmation), réglages (rythme, méthodologie globale, toggle TTS avec persistance et effet sur un Feynman lancé ensuite), export JSON téléchargé et lisible — aucun écart. Bloc 9.1 déclaré terminé (`npm run check` + `test:canary` verts, canaris du bloc posés, test navigateur passé) ; commit à suivre. | Confirmation explicite de l'humain, conformément à la boucle PLAN.md §0 (étape 5 avant l'étape 7 commit). | — |
| 2026-07-09 | Bloc 9.1 — archivage/suppression matière+chapitre, export JSON, réglages, récitation | (1) **Suppression gardée = transaction applicative ordonnée, pas de `onDelete: cascade`** en DDL — `S1.deleteChapter` (errorEntry → studySession → studyCycle → reviewCard → correctionGuide → section → chapter, une seule transaction) réutilisé par `S9.deleteSubject` (boucle sur les chapitres puis suppression du subject) : la destruction reste visible et testée en code, aucun futur `DELETE` ailleurs ne peut cascader par accident (aucune FK du schéma n'avait de cascade avant ce bloc — un `DELETE` brut aurait simplement échoué). (2) **Archivage chapitre = pur flip de statut, aucune cascade** : `loadActiveReviewCards`/`loadReadyStudyCandidates` (S5, `planner.ts`) et le candidat de `S3.lazyScheduler` (`guide.ts`) ne filtraient QUE `subject.statut`, jamais `chapter.statut` — gap symétrique à celui du Bloc 8.4 (chapitresNonTries), corrigé en ajoutant `eq(chapter.statut, "actif")` aux trois requêtes plutôt que de geler chaque ReviewCard de section une par une (S9.archiveSubject faisait déjà ça : zéro cascade, uniquement des filtres — même doctrine reprise pour le chapitre, plus simple que prévu en récitation). (3) TTS on/off : `PlannerConfig.ttsActive BOOLEAN DEFAULT true` (ARCHITECTURE v0.9, migration `0006_clumsy_the_stranger.sql`) — réutilise la table de config existante plutôt qu'une nouvelle table pour un seul booléen ; `FeynmanChat` (Bloc 7.2) ne portait qu'un `useState` local jamais persisté, devient la valeur INITIALE du toggle (le toggle en session reste libre, ne réécrit pas le réglage). (4) `exportUserData` (S9) inclut tout `auditEvent`/`promptLog` sans filtre par utilisateur — ces deux tables n'ont pas de `user_id` (doctrine mono-utilisateur, ADR 6, même raison que DeferralLog/RefileItem/QueueOrder) : il n'y a jamais qu'un seul utilisateur dont ces journaux pourraient parler. Téléchargement via Route Handler (`app/api/export/route.ts`, `Content-Disposition`), pas un Server Action (pas de vraie réponse HTTP fichier possible autrement). (5) `StrongConfirmDialog` (U8, nom à retaper) ajouté à côté du `ConfirmDialog` léger existant, dans le même fichier — même composant de la famille FUNCTIONS §6.1, deux variantes. (6) Réglages (U1) réactivé, `/reglages` construit : rythme + méthodologie globale réutilisent les Server Actions déjà existantes depuis l'onboarding (`updatePlannerConfigAction`/`updateMethodologieGlobaleAction`, jamais câblées après le Bloc 0.2) — pas de duplication. Pas de composant Switch shadcn (absent du dépôt) : toggle TTS en bouton à deux états, même geste que le toggle local déjà existant dans FeynmanChat. | (1)-(2) tranchés avec l'humain (AskUserQuestion) en récitation, avant code — recommandations retenues. (3)-(6) tranchés en récitation (lecture directe de FUNCTIONS §3 S1/S9, USER_FLOW É6.4/P7, ADR 6). | (1) `onDelete: "cascade"` en DDL (rejeté : cascade silencieuse activable par n'importe quel futur DELETE, cf. ci-dessus). (2) Geler chaque ReviewCard de section à l'archivage du chapitre (rejeté après vérification : inutile, les filtres suffisent, exactement comme pour la matière). |
| 2026-07-13 | Bloc 9.2 (préalable) — archive/désarchive : la doctrine « pur flip de statut » du Bloc 9.1 est supersédée | Contradiction relevée en préparant le Bloc 9.2 : USER_FLOW É6.4 et FUNCTIONS §S6 (l.87, « dégel : FSRS recalcule depuis les dates réelles » ; S1.archive listé comme l'un des 3 appelants de `S6.freeze`/`unfreeze`) exigent un recalcul au désarchivage, que la décision Bloc 9.1 ci-dessus (filtres seuls, aucun appel à S6) ne fournissait pas. Tranché avec l'humain (AskUserQuestion) : implémenter le recalcul plutôt que réviser la doc pour matcher le code. Algorithme retenu (2ᵉ AskUserQuestion, faute d'historique de révisions persisté pour utiliser `ts-fsrs.reschedule()`) : décaler `due` de la durée réelle du gel (`archivedAt` → maintenant), `stability`/`difficulty`/`reps`/`lapses` intacts — aucune note système fabriquée (ADR 3 respectée). `Chapter.archived_at`/`Subject.archived_at TIMESTAMPTZ NULL` ajoutés (migration `0007_typical_dragon_man.sql`, ARCHITECTURE v0.10) ; `S6.unfreeze` prend un `frozenSince: Date \| null` optionnel ; `S1.archiveChapter`/`unarchiveChapter` et `S9.archiveSubject`/`unarchiveSubject` appellent désormais réellement `S6.freeze`/`unfreeze` (3ᵉ appelant du trio FUNCTIONS §S6 enfin câblé, `S2.setImportance` restant hors scope, DECISIONS.md Bloc 3.3). Cascade matière→chapitre bornée à `chapter.statut = actif` pour ne pas re-décaler un chapitre déjà archivé indépendamment. `planner.canary.test.ts` modifié en conséquence (autorisation explicite de l'humain dans cet échange, canari non déplacé/renommé — seule l'assertion obsolète change) ; `schema.canary.test.ts` (colonnes attendues) mis à jour pour les 2 nouvelles colonnes. `npm run check` (×2) + `test:canary` verts. | Root cause exigée avant de continuer le Bloc 9.2 plutôt que de bâtir sur une doc et un code en contradiction (AGENTS.md §1) ; décisions produit/algorithme non devinées, tranchées avec l'humain (AGENTS.md §6). | Mettre à jour USER_FLOW.md pour matcher le pur flip de statut (rejeté par l'humain). Simuler une note « Good » système au dégel via `scheduler.next` (rejeté : falsifierait `reps`/`difficulty` sans qu'aucun humain n'ait noté quoi que ce soit, contraire à l'esprit ADR 3). Table d'historique de révisions pour utiliser `ts-fsrs.reschedule()` nativement (rejeté : ADR-level, hors scope de ce fix). |
| 2026-07-13 | Bloc 9.2 — audit états vides/erreur (règle transversale 7) vs USER_FLOW, gaps corrigés | Agent de recherche dépêché sur les 15 écrans app/(app)+(focus) contre USER_FLOW.md, 5 gaps confirmés et corrigés : (1) **É1.0** — matière inexistante affichait un texte statique sans action ; lien `[Créer une matière]` → /curriculum ajouté (`import-wizard.tsx`). (2) **É1.3** — l'échec LLM du sectionnement bascule déjà silencieusement sur P6 (secours mécanique automatique, comportement voulu) mais n'offrait aucun moyen de retenter l'IA une fois basculé ; bouton « Réessayer avec l'IA » ajouté dans la branche `method === "mecanique"`, réutilise `triggerPropose` existant. (3) **É1.5** — `regenerateRubricAction` n'avait aucun `try/catch` et aucune route `(app)/error.tsx` n'existe : un échec LLM à la régénération faisait planter l'écran entier (perdant au passage le formulaire d'édition manuelle qui EST la voie de secours de la règle transversale 1). Action convertie en forme `useActionState` (même pattern que `validateRubricAction`), erreur affichée à côté du bouton, formulaire manuel intact. (4) **É6.1** — `S3.generateBatch` existe depuis le Bloc 4.1 mais n'était câblé nulle part (le commentaire du Bloc 9.1 listait « générer en lot » comme fait, à tort) ; bouton + Server Action ajoutés, résumé « X/Y générées, Z en échec » (chaque échec reste `rubrique_a_valider`, rédigeable à la main — pas de nouveau chemin d'erreur, `generateMany` isole déjà chaque échec par `Promise.allSettled`). (5) **É2.0** — état vide sans « prochaine échéance » ni CTA réels (texte statique). `S5.nextDeadline` ajouté (min `due` des ReviewCards actives) ; CTA `[Importer un chapitre]` ajouté. Le second CTA (« avancer une étude de demain ») a ouvert la découverte ci-dessous. | Trouvailles d'un audit ciblé, gaps fonctionnels contre des exigences explicites de USER_FLOW.md (règles transversales 1 et 7), pas des différences de style. | É1.3 : exposer aussi l'échec brut avant bascule mécanique (rejeté — la bascule automatique est le comportement voulu par USER_FLOW, seul le retour arrière manquait). |
| 2026-07-13 | Bloc 9.2 — bug pré-existant trouvé en corrigeant (5) : le report d'une nouvelle étude ne perdait pas son slot | En creusant le CTA « avancer une étude de demain » (USER_FLOW É2.0), constat que `S5.todayQueue` (Bloc 6.3) contredisait déjà la règle juste au-dessus dans le même document : *« Nouvelle étude reportée : le slot du jour est perdu — aucune section ne la remplace (sinon reporter ne coûte rien et le vivier défile sans travail) »*. Le code filtrait la candidate reportée de `newStudyCandidates` puis prenait `slice(0, nouvellesParJour)` sur ce qui restait — la candidate suivante du vivier prenait donc systématiquement la place libérée, rendant le report gratuit. Signalé et tranché avec l'humain (AskUserQuestion) avant de continuer : corriger maintenant plutôt que différer, risque de régression sur le mécanisme quotidien accepté. Fix : `loadReadyStudyCandidates` renvoie désormais le vivier complet (non filtré) ; `todayQueue` calcule `slotsLost` (candidates encore `prete` reportées aujourd'hui) et réduit `nouvellesParJour` d'autant au lieu de simplement exclure les candidates reportées de la liste avant troncature. Un report de carte re-file (même `item_type="etude"` dans DeferralLog) ne compte pas : sa section n'est plus `prete` une fois le cycle entamé, donc absente du vivier — `slotsLost` ne la voit pas. Nouveau test `planner.test.ts` verrouillant le non-remplacement (4 candidates, plafond 3, 1 reportée ⇒ 2 affichées, jamais 3). | Contradiction interne à USER_FLOW.md entre la règle énoncée et le code déjà committé (Bloc 6.3) — signalée avant d'agir (AGENTS.md §1), tranchée avec l'humain plutôt que corrigée silencieusement en marge d'un autre fix. | Laisser filer et noter pour plus tard (rejeté par l'humain — corrigé immédiatement, avant la mécanique d'avance qui aurait sinon hérité du même défaut). |
| 2026-07-13 | Bloc 9.2 — S5.advanceFromBacklog + DeferralLog.avance (USER_FLOW É2.0, CTA « avancer une étude de demain ») | Mécanique complète construite après un premier essai de réutilisation nue de `DeferralLog` jugé insuffisant (aurait rouvert le bug du dessus : une dette d'avance datée de demain, une fois la section étudiée, disparaît du vivier de demain et ne serait plus jamais comptée par le filtre `allStudyCandidates.some(...)` qui protège justement contre les faux positifs des cartes re-file). Colonne `DeferralLog.avance BOOLEAN` ajoutée (migration `0008_swift_stardust.sql`, ARCHITECTURE v0.11) pour distinguer les deux : un report normal (compté seulement si la section est encore dans le vivier ce jour-là) et une dette d'avance (comptée inconditionnellement). `S5.advanceFromBacklog(sectionId, date)` pose la dette sur `date+1` sans démarrer l'étude elle-même (délégué à `S4.start` via la redirection `/etude/[id]` de la Server Action, aucune duplication de logique) ; `S5.nextBacklogCandidate` expose la candidate que la file du jour montrerait en premier si le plafond n'était pas atteint (même tri que P7 : importance DESC, proximité d'examen, ordre curriculum) pour l'afficher sur le bouton. Pas de vérification de propriété/`userId` sur `advanceFromBacklog` : même doctrine que `defer`/`refile` existants (DeferralLog est mono-utilisateur, ADR 6). | Root cause exigée avant de committer une mécanique bâtie sur un fondement bogué (AGENTS.md §1) ; conception vérifiée contre l'API réelle de `ts-fsrs`/le schéma existant avant d'écrire du code, tranchée avec l'humain (AskUserQuestion) une fois le coût réel (migration) mesuré. | Réutiliser `DeferralLog.item_type` avec une 3e valeur d'enum partagée avec `RefileItem` (rejeté : `RefileItem` n'a aucun usage pour cette valeur, sémantique empruntée à une autre table plutôt qu'un champ propre). Ignorer la distinction et accepter un léger sur-comptage (rejeté : aurait rouvert silencieusement le bug qu'on vient de corriger dans l'entrée précédente). |
| 2026-07-13 | Bloc 9.3 — accessibilité/mobile, performance, seeds de démo, récitation | (1) **« Application mobile » (ARCHITECTURE §12, hors périmètre)** clarifié comme ne visant pas ce bloc : le hors-périmètre porte sur une app native, pas sur l'usage responsive au navigateur mobile que PLAN.md Bloc 9.3 demande explicitement (« sur téléphone dans le train ») — signalé en récitation, aucune ligne de code native ajoutée. (2) **Performance/N+1 sur la file** : audit de `S5.todayQueue`/`horizon`/`attentionBadges` (`planner.ts`) et de leur appelant (`app/(app)/page.tsx`) — déjà exclusivement des jointures/`inArray` groupés, aucune requête en boucle par item. Rien à corriger ; pas de canari de comptage de requêtes ajouté (aurait verrouillé un détail d'implémentation sans bug constaté). (3) **Audit mobile ciblé** (pas une passe exhaustive de tous les écrans) : deux `grid-cols-2`/`flex` non responsives cassaient réellement sur un viewport téléphone — `RubricEditor` (U14, `grid grid-cols-2` fixe → `grid-cols-1 md:grid-cols-2`) et les formulaires matière de `curriculum-ui.tsx` (3 champs `flex-1` cote à cote → `flex-col sm:flex-row`, 2 occurrences : ajout/édition). Cibles tactiles des deux boutons icône les plus utilisés en mobile (réordonnancement `DailyQueue`, micro `PushToTalkRecorder`) portées de `size="icon"` (32px) à `size="icon-lg"` (36px, le plus grand token shadcn existant — pas de nouvelle taille inventée). `app/layout.tsx` : `lang="en"` → `"fr"` (app entièrement en français, lu par les lecteurs d'écran) + titre/description placeholders « Create Next App » remplacés. Composants `src/components/ui/*` (shadcn vendored, ex. `SidebarTrigger` en `icon-sm`) volontairement non retouchés — TECH_MAPPING §0 : ne pas réécrire ce qui est délégué. (4) **Seed de démo** (`src/db/seed.ts`, script CLI `npm run db:seed -- <userId>`, jamais une Server Action/route) : peuple 3 matières / 3 chapitres / 8 sections couvrant les 4 badges d'attention, une révision en retard, une nouvelle étude prête et une échéance à venir — un seul utilisateur (userId Supabase existant), pas d'idempotence (hors scope, note `ponytail:` dans le fichier). Exécuté via `node --experimental-strip-types` (natif Node 22, TECH_MAPPING §0 « natif d'abord ») plutôt qu'une dépendance `tsx`/`ts-node` ; le script ne peut pas importer `src/db/index.ts` tel quel (imports relatifs sans extension, convention bundler Next.js incompatible avec l'ESM natif de Node) — client Drizzle recréé à l'identique dans le script plutôt que de changer les extensions d'un fichier partagé pour un besoin d'outillage qui lui est propre. `tsconfig.json` : ajout de `allowImportingTsExtensions` (safe sous `noEmit: true`, déjà actif) pour que `tsc --noEmit` accepte les imports `./schema.ts` du script. Vérifié en conditions réelles (utilisateur `auth.users` jetable créé/seedé/vérifié en base/nettoyé dans cette session, jamais laissé en base). (5) **Aucun nouveau canari** : PLAN.md ne rattache aucun `@canary` à Bloc 9.3 spécifiquement (contrairement à 9.1) ; les 2 canaris de Phase 9 sont déjà posés (DECISIONS.md, entrée Bloc 9.1). `npm run check` + `test:canary` verts. | (1) et (5) signalés en récitation avant code plutôt que tranchés silencieusement (AGENTS.md §1) ; (3)-(4) ancrés sur un audit réel du code existant (pas de correctifs spéculatifs) conformément à la doctrine ponytail de cette session. | Passe a11y exhaustive de tous les écrans (rejeté : hors du périmètre concret listé par PLAN.md — la triage-list/correction-view/blurting-editor déjà vérifiées mobile-friendly par construction, pas de bug trouvé). Nouvelle taille de bouton icône >36px sur-mesure (rejeté : au-delà du système shadcn, TECH_MAPPING §0). Dépendance `tsx`/`ts-node`/`faker` pour le seed (rejeté : natif/maison suffisant, TECH_MAPPING §0). |
| 2026-07-13 | Phase 9 — sortie confirmée | L'humain confirme avoir déroulé le scénario de sortie de PLAN.md Phase 9 (fin de semestre simulée, 15 minutes d'usage sur téléphone), aucun écart signalé. Phase 9 officiellement close avant d'ouvrir la Phase 10. | Confirmation explicite de l'humain (AskUserQuestion), conformément à la règle PLAN.md « on ne passe jamais à la phase suivante sans critère de sortie rempli » — même doctrine que la clôture de Phase 8. | — |
| 2026-07-13 | PLAN.md — clôture, Phase 10 abandonnée | L'humain déclare le plan officiellement fini : Phase 10 (consolidation evals, campagne d'itération des prompts, mise en service formelle) **volontairement abandonnée**, sans être exécutée bloc par bloc. PLAN.md marqué CLOS (v0.3, en-tête + journal des versions) — conservé comme historique des phases 0–9, ne gouverne plus les sessions. Protocole de session correspondant retiré : CLAUDE.md §1 (ex-« boucle par bloc ») réécrit en protocole par tâche (plus de récitation formelle obligatoire, plus de règle « une session = un bloc », commit par tâche cohérente au lieu d'un commit par bloc) ; AGENTS.md §1/§4/§5 mis à jour en conséquence (PLAN.md retiré de la hiérarchie des documents gouvernants, DoD renommée « d'une tâche »). | Confirmation explicite de l'humain (AskUserQuestion) : l'usage quotidien réel de l'app (Phases 5-9) a déjà rempli l'objectif produit ; poursuivre la Phase 10 sous la discipline bloc-par-bloc n'apporterait rien que l'usage courant ne couvre déjà (evals/prompts s'itèrent au fil de l'eau désormais, hors plan formel). | Exécuter la Phase 10 telle quelle avant de clore (rejeté par l'humain) ; garder la boucle par bloc comme protocole par défaut avec PLAN.md simplement marqué archivé (rejeté : l'humain veut le protocole lui-même allégé, pas seulement le document tamponné). |
| 2026-07-13 | curriculum — file de génération de rubriques, remplace le verrou local de `SectionRow` | Le verrou par section (`submitting = genPending \|\| manPending`, commit 2ea9e73) est remplacé par une file visible et partagée : `src/components/rubric-generation-queue.ts` (store pur, runners injectés — ne dépend pas de Next.js/Server Actions, testable en Vitest sans mock lourd) + `RubricQueuePanel` dans `curriculum-ui.tsx`. Un seul item traité à la fois, les suivants en `en_attente` sont annulables (retrait avant tout appel réseau) ; une fois `en_cours`, pas d'annulation (pas d'`AbortSignal` dans `src/llm/client.ts`, feasability actée en amont avec l'humain). **`generateRubricAction`/`createManualRubricAction` (`app/(app)/actions.ts`) ne redirigent plus** vers `/section/[id]/rubrique` après succès — c'était le seul appelant, et un `redirect()` sur le premier item terminé aurait éjecté l'utilisateur de `/curriculum` en pleine file (plusieurs items encore `en_attente`/`en_cours`). La validation reste à un clic via `[Valider la rubrique]`, déjà rendu par `SectionRow` une fois le statut `rubrique_a_valider`. 4 canaris posés (`rubric-generation-queue.canary.test.ts`) : traitement strictement séquentiel, dédoublonnage par section (même invariant que l'ancien verrou), annulation avant dispatch, isolation d'échec (un item en erreur ne bloque pas les suivants). | Demandé explicitement par l'humain après une évaluation de faisabilité actée en conversation (verrous DB/session-unique conservés tels quels — hors de portée d'une file client ; seul le verrou UI local de `/curriculum` remplacé, 1 seul des 8 verrous du commit 2ea9e73 concerné). Le retrait du `redirect()` est un écart par rapport au comportement Bloc 4.1 d'origine, signalé ici plutôt que laissé implicite : aucune action perdue, seul le moment de la navigation change (manuel via bouton existant au lieu d'automatique). | Garder le `redirect()` et n'enfiler qu'un seul item à la fois côté file (rejeté : aurait limité la file à un intérêt cosmétique, la navigation forcée aurait quand même interrompu tout traitement d'un deuxième item derrière). Ajouter un `AbortSignal` pour annuler un item `en_cours` (rejeté : hors scope, nécessiterait de faire remonter l'annulation jusqu'à L0/OpenRouter). |
| 2026-07-13 | UI — remplacement shadcn/Radix/Tailwind par Astryx | Migration incrémentale, surface par surface, de shadcn/ui (Radix) + classes Tailwind vers **Astryx** (`@astryxdesign/cli`, `@astryxdesign/core`, `@astryxdesign/theme-neutral`, déjà installés par l'humain). shadcn/Radix restent en place sur les surfaces pas encore migrées ; TECH_MAPPING §0/§4/§6 mis à jour pour documenter Astryx comme délégation UI actée (v0.4). Routing (App Router, route groups `(app)`/`(focus)`) et logique de service inchangés — le remplacement ne touche que `src/components/` et le styling, surface par surface, avec vérification light/dark et screenshot avant de passer à la suivante. | Tranché avec l'humain (AskUserQuestion) avant toute exécution de commande `npx astryx` : contradiction explicite avec AGENTS.md §2.1 (dépendance hors TECH_MAPPING, shadcn/Tailwind nommés comme délégation imposée) signalée avant d'agir ; package vérifié installé (`package.json`) et doc du fournisseur consultée (URL fournie par l'humain) avant confirmation. | Refuser la migration et rester sur shadcn/Radix/Tailwind ; migrer sans mettre à jour TECH_MAPPING/DECISIONS (aurait laissé la doc et le code diverger) |
| 2026-07-13 | UI Astryx — `confirm-dialog.tsx` migré sans toucher ses appelants | `ConfirmDialog`/`StrongConfirmDialog` (`src/components/confirm-dialog.tsx`) réécrits sur `Dialog`/`useState` Astryx (l'AlertDialog Radix composable par `render={trigger}` n'a pas d'équivalent Astryx — Dialog est piloté par `isOpen`/`onOpenChange`). L'API publique du composant est conservée à l'identique (`trigger: ReactElement`) : le trigger est toujours cloné (`cloneElement`) pour y injecter l'ouverture, ce qui marche aussi bien avec un bouton shadcn qu'Astryx (les deux spread `onClick` sur un `<button>` natif). Résultat : les 2 surfaces pas encore migrées qui utilisent ce dialogue (`chapitre/[id]/chapter-editor-screen.tsx`, `error-notebook.tsx`) continuent de fonctionner sans modification, avec leurs boutons shadcn en trigger — seul `curriculum-ui.tsx` (déjà migré) a basculé son trigger `Supprimer` vers `Button` Astryx. Vérifié en navigateur (compte e2e jetable, Mailpit local) : `StrongConfirmDialog` sur `/curriculum`, light + dark. | Éviter qu'un composant partagé (U8) ne force la migration de 2 surfaces entières hors scope de la tâche du jour ; cohérent avec la doctrine « une surface à la fois » déjà actée. TECH_MAPPING §4.1 (ligne U8) mis à jour : Dialog Astryx pour `confirm-dialog.tsx`, Toast shadcn toujours en attente. | Migrer aussi les triggers de `chapter-editor-screen.tsx`/`error-notebook.tsx` dans la foulée (rejeté : ces écrans ont d'autres composants shadcn non migrés, ç'aurait été une migration partielle et incohérente de 2 surfaces en plus de la tâche demandée) |
| 2026-07-14 | UI Astryx — migration achevée (toutes surfaces, ~25 fichiers) | Fin du chantier ouvert le 2026-07-13 : les 25 fichiers restants (`chapter-editor-screen.tsx`, `import-wizard.tsx`, `reglages-ui.tsx`, `onboarding-steps.tsx`, écrans `(focus)`, et une dizaine de composants — badges, listes, éditeurs) migrés vers Astryx. À la demande explicite de l'humain, le nettoyage est allé au-delà des seuls composants React : les classes Tailwind héritées de shadcn (`text-muted-foreground`, `bg-primary`, `border-input`, etc.) ont aussi été remplacées par les tokens Astryx (`text-secondary`, `bg-accent-bg`, `border-border`...) partout, y compris dans le code déjà migré la veille. `globals.css` : bloc de tokens couleur shadcn (`:root`/`.dark`/`@theme inline`) et `@import "shadcn/tailwind.css"` supprimés ; seule règle ajoutée, un default `border-color: var(--color-border)` sur `*` (Astryx `reset.css` laisse la bordure nue à `currentColor`, contrairement à shadcn — évite de devoir recolorer individuellement les dizaines de `border`/`border-t` déjà présents). `src/components/ui/` (14 fichiers), `components.json` supprimés ; `shadcn`, `@base-ui/react`, `class-variance-authority`, `tw-animate-css` retirés de `package.json` (`clsx`/`tailwind-merge` conservés : génériques, encore utilisés par `cn()` hors shadcn). Deux bugs réels trouvés en vérification e2e (pas seulement `npm run check`, insuffisant ici) : (1) `Button href=... as={Link}` utilisé dans un **Server Component** (`app/(app)/page.tsx`, `curriculum/page.tsx`) fait planter le rendu (RSC ne peut pas sérialiser une référence de composant client comme prop) — corrigé en gardant `href` seul (ancre native) sur ces deux écrans, `as={Link}` réservé aux Client Components ; (2) un bouton de navigation avec `href` rend un vrai `<a>` (`role="link"`), pas `role="button"` — comportement d'accessibilité correct mais cassait les sélecteurs `getByRole("button", ...)` de `auth-flow.spec.ts`/`import-wizard.spec.ts` sur Suivant/Passer, mis à jour vers `getByRole("link", ...)`. | Le remplacement des classes Tailwind (au-delà des seuls composants) a été explicitement tranché avec l'humain (AskUserQuestion) plutôt que deviné, vu l'ampleur du diff que ça impliquait. Les deux bugs RSC/a11y n'étaient détectables ni par `tsc` ni par les tests unitaires (aucun test ne rend ces pages en SSR réel) — seule la suite Playwright complète (`npm run check:full`, pas juste `check`) les a révélés ; corrigés avant de clore la tâche plutôt que livrés en connaissance de cause. | Ajouter une règle globale `* { border-color }` ET recolorer quand même chaque `border`/`border-t` individuellement (rejeté : redondant, la règle globale suffit et limite le diff) ; contourner le crash RSC en convertissant `app/(app)/page.tsx`/`curriculum/page.tsx` en Client Components (rejeté : violerait la doctrine Server Components par défaut pour un simple bouton de nav) ; laisser les tests e2e casser et le signaler seulement (rejeté : la tâche demandée était « terminer la migration », un e2e rouge n'est pas terminé) |
| 2026-07-15 | REVAMP.md v0.3 — passage du cycle d'étude v1→v2 (rituel lecture/blurting/Feynman), §2 | Changement de doctrine Machine B (étude uniquement, Machine C/révision intacte), 5 ruptures actées avec l'humain : (A) divulgation toujours complète dès la 1ʳᵉ correction, `presentCorrection`/P10 supprimée (plus aucun appelant avec masquage à opérer) — sa suppression emporte celle de `presentCorrection.canary.test.ts` (testait la branche `retryAttendu: true` devenue morte), autorisée par la règle générale de l'humain « supprimer une fonction qui ne sert plus », reconfirmée explicitement au moment de coder cette étape (AGENTS §2.5). (B) Exactement 2 passes de blurting max, la 2ᵉ immédiate dans la même séance, jamais de re-file/délai en étude (`resolveOutcome` n'appelle plus `plannerService.refile` en étude, boucle vers `lecture`). (C) Feynman toujours atteint après la 2ᵉ passe (ou après la 1ʳᵉ si acquis) — `beginFeynman` perd son garde sur verdict insuffisant et son audit `override_verdict`. (D) La lecture du cours devient un état à part entière de `StudyCycle.etat` (`lecture`, migration d'enum), deux occurrences par cycle (initiale, puis relecture ciblée avec diff avant la 2ᵉ passe) — nouvelle fonction `terminerLecture` (transition seule `lecture→blurting`) et nouveau composant `lecture-view.tsx`/U25 ; structurel, sans minuteur. (E) Feynman obligatoire pour toute importance 2 à 5, `[Valider sans Feynman]` et la branche correspondante de `validateSection` supprimés. **§2.3bis** (portée de l'abandon vis-à-vis de l'immutabilité des sessions, ADR 6) tranché avec l'humain (AskUserQuestion) : réutilisation telle quelle de `S4.abandon` — ferme le cycle, aucune `StudySession` supprimée (trace DB immuable conservée, ADR 6 intact), rien de visible ni comptabilisé pour la tentative suivante ; aucune nouvelle fonction d'annulation, aucune dérogation à ADR 6. Documents mis à jour en conséquence AVANT le code (ARCHITECTURE v0.12, USER_FLOW v0.5, FUNCTIONS v0.4) ; AGENTS.md §2 point 6 également corrigé (hors du tableau REVAMP §5, signalé) : la mention « divulgation contrôlée → P10 » y était devenue fausse une fois P10 supprimée. | Décisions produit (nombre de passes, obligation du Feynman, lecture in-app) tranchées par l'humain, seul habilité (AGENTS §6) ; §2.3bis spécifiquement soumis en question fermée (a/b) avant tout code touchant l'abandon, un invariant documenté (ADR 6) ne se déroge jamais en silence. | (§2.3bis) Suppression physique des lignes `StudySession` de la tentative abandonnée (rejetée : aurait dérogé à ADR 6 — INSERT only — pour un gain purement cosmétique, aucune trace n'étant de toute façon visible en avant avec l'option (a) retenue). |
| 2026-07-15 | Suppression de la dualité étude\|révision (Machine B/C fusionnées) | Discussion avec l'humain (post-hoc, après REVAMP.md ci-dessus) : la distinction Machine B (étude complète) / Machine C (révision = simple blurting noté FSRS) était incohérente du point de vue apprentissage — un cycle d'étude renforce autant la mémoire qu'une révision, mais seule la révision (la version la MOINS complète : pas de lecture, pas de Feynman) alimentait FSRS. Décision : **une seule machine désormais** — toute répétition d'une section (1ʳᵉ fois ou Nᵉ fois) traverse le cycle complet (lecture → blurting ≤2 passes → Feynman → bilan). La note FSRS (Again/Hard/Good/Easy) migre de Machine C vers la clôture du bilan Feynman (É3.5), où elle devient **une condition nécessaire à la validation** (on ne peut pas clore sans noter) — remplace `S4.rateRevision`, absorbé dans `S4.validateSection` (nouveau paramètre `note` obligatoire). `S6.createOrRate` (nouveau, compose `createCard`+`rate` existants sans les modifier — canaris `review.canary.test.ts` intacts) : crée la carte si absente (1ʳᵉ validation) PUIS lui applique la note dans la foulée (une carte neuve `createEmptyCard` ne doit jamais rester exposée sans note réelle, son `due` serait "maintenant"). Conséquences actées : (1) **auto-note automatisée depuis le bilan LLM explicitement rejetée** — les 4 niveaux Again/Hard/Good/Easy modélisent un effort de rappel SUBJECTIF, pas une couverture de contenu ; un mapping programmatique depuis démontré/récité/non_abordé perdrait la distinction Hard/Easy (le cœur de ce qui rend FSRS meilleur qu'un simple pass/fail) et dériverait optimiste avec le temps (le carnet d'erreurs améliore les corrections). (2) **Aucune migration de schéma** : `ReviewCard` inchangée (ses champs sont agnostiques de la forme du cycle qui produit la note) ; `StudyCycle.type` (etude/revision) conservé mais devient un LABEL pur (badge « Nᵉ rappel », tri du Planificateur ADR 8) sans plus piloter aucune branche de comportement — Machine A (`section.statut` prete/en_revision) survit intacte, c'est elle que le Planificateur consulte pour distinguer nouvelles études et révisions dues, pas plus. (3) **Le mécanisme « 2 Again consécutifs ⇒ retour à l'étude complète » disparaît** : il n'existait que pour retomber sur une version complète depuis une version allégée — cette version allégée n'existe plus, toute répétition EST déjà la version complète. (4) **`Again ⇒ re-file intra-journée` conservé tel quel**, malgré le changement de coût (retenter un cycle complet le jour même coûte bien plus cher que retenter un simple blurting) — assumé explicitement par l'humain plutôt que retravaillé. (5) Route `/revision/[cardId]` supprimée, tout passe par `/etude/[sectionId]` (même machine, même écran). | Diagnostic initial de l'humain (le blurting seul en révision est un signal d'apprentissage plus faible que Feynman, donc noter FSRS uniquement sur ce chemin est incohérent) ; les 3 points de conception (auto-note vs bilan LLM, portée du changement, sort du mécanisme Again) tranchés explicitement avant tout code. | Dériver la note FSRS automatiquement du bilan Feynman (rejeté, §1 ci-dessus — perte de la granularité d'effort subjectif que FSRS modélise) ; garder Machine C comme chemin allégé optionnel en plus du cycle complet (rejeté : réintroduirait exactement la dualité qu'on supprime) ; retravailler le coût du re-file `Again` maintenant (rejeté par l'humain — accepté tel quel malgré le nouveau coût). |
| 2026-07-15 | Retour sur « lecture structurelle, sans minuteur » (REVAMP.md v0.3, point D) | Demande de l'humain : ajouter un minuteur de 30s à la fin de la lecture, avec échappatoire manuelle, avant le passage au blurting — contredit directement le point (D) du REVAMP.md v0.3 ci-dessus (« structurel, sans minuteur »), signalé avant tout code (AGENTS.md §1) puis confirmé explicitement par l'humain (AskUserQuestion, « Oui, ajoute le minuteur », appliqué aux deux occurrences de lecture — initiale ET relecture ciblée). Implémentation purement côté client dans `LectureView`/U25 : `[Je suis prêt, je blurte]` bascule l'écran vers un compte à rebours (30s, décompte affiché), `[Passer maintenant]` l'interrompt ; dans les deux cas, `S4.terminerLecture` (transition seule `lecture→blurting`, inchangée) est appelée via `startTransition` — pattern déjà utilisé par `FeynmanChat`/`closeFeynmanAction` pour un Server Action invoqué hors `<form>` (cf. `node_modules/next/dist/docs`). Aucun nouvel état `StudyCycle.etat`, aucune migration : le minuteur n'existe que dans le rendu client de l'écran déjà existant. Portée volontairement limitée à `/etude` (Machine B) — ne concerne ni `MarkdownViewer` en lecture seule ailleurs (curriculum, carnet d'erreurs) ni aucun autre écran. | Décision produit tranchée explicitement par l'humain (AGENTS §6), qui inverse un arbitrage précédent lui aussi tranché avec l'humain — aucune des deux décisions n'est résolue par déduction ou supposition. | Faire du compte à rebours un nouvel état persisté de `StudyCycle.etat` (rejeté : aucun besoin de survivre à un rechargement de page pour une pause de 30s, aurait exigé une migration pour un comportement purement cosmétique). |
| 2026-07-15 | Retrait du `DiffList` de l'écran de relecture ciblée (É3.1, U25) | Demande de l'humain : supprimer le diff de la correction précédente affiché en vis-à-vis pendant la relecture ciblée — contredit un comportement explicitement décrit dans USER_FLOW.md/ARCHITECTURE.md/FUNCTIONS.md (le diff « pour orienter ce qui reste à combler », REVAMP.md §2.7), signalé avant tout code (AGENTS.md §1) puis confirmé explicitement par l'humain (AskUserQuestion : suppression complète code + docs, plutôt qu'un simple masquage visuel). `LectureView`/U25 : prop `diff?: MergedDiffPoint[]` remplacée par `relecture?: boolean` (cosmétique, pilote uniquement le badge « Lecture »/« Relecture ciblée ») ; import et rendu de `DiffList` (U16) retirés, layout deux colonnes redevenu une seule colonne dans les deux contextes. `app/(focus)/etude/[sectionId]/page.tsx` passe désormais `relecture={tentative === 2}` au lieu de `diff`. `S4.lectureContext` **volontairement inchangée** : continue de calculer et renvoyer le diff de la correction précédente (typé, testé par `session.test.ts`) — simplement plus consommé côté UI ; le retoucher aurait élargi le diff au-delà de l'écran demandé, pour un service déjà couvert par des tests qui n'ont rien à voir avec cette tâche. | Contradiction avec un comportement documenté et actée comme telle avant d'agir (AGENTS.md §1) ; le service `lectureContext` n'a pas été touché pour garder le diff de la tâche au périmètre demandé (« l'écran »), pas la couche service. | Retirer aussi `diff` de `S4.lectureContext`/`LectureContext` (rejeté par prudence, ponytail : aurait élargi le changement à un service testé indépendamment de l'écran, sans bénéfice demandé) ; masquer le `DiffList` par CSS plutôt que le retirer du rendu (rejeté par l'humain, qui a choisi la suppression complète). |
