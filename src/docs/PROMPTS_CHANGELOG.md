# PROMPTS_CHANGELOG.md — Journal des versions de prompts

> Une entrée par version de fichier sous `/prompts`. Toute modification d'un prompt ⇒ nouvelle version (`.vN+1.md`) + entrée ici + `npm run evals` + verdict (AGENTS.md §2.4).

| Date | Fichier | Changement | Verdict |
| ---- | ------- | ---------- | ------- |
| 2026-07-06 | `rubrique.v1.md` | Version initiale (bloc 4.1). Non consignée ici au moment de sa création — ce fichier n'existait pas encore ; entrée ajoutée rétroactivement au moment où `PROMPTS_CHANGELOG.md` est créé (bloc 3.2), sans repasser les evals (aucun changement de contenu). | — (evals non exécutés, hors scope bloc 3.2) |
| 2026-07-07 | `sectionnement.v1.md` | Version initiale (bloc 3.2) : appel L1, sortie = sections par indices du plan (Titre 2, sinon Titre 1). | — (pas d'`evals/sectionnement/` : nécessite l'écran de tri du bloc 3.3 pour produire un découpage de référence validé par l'utilisateur) |
| 2026-07-08 | `correction_erreurs.v1.md` | Version initiale (bloc 5.1) : appel L3, sortie = diff par point (référencé par `point_index`, 1-based) + erreurs candidates (récidive par `recidive_index`) — pas de champ verdict, calculé en code (DECISIONS.md). | — (pas d'`evals/correction/` : nécessite de vrais blurtings, produits par un usage réel de l'app à partir du bloc 5.2/5.3, cf. CLAUDE.md §2 données réelles) |
| 2026-07-09 | `transcription.v1.md` | Version initiale (bloc 7.1) : appel L6, audio transmis hors prompt (content-type `input_audio`), sortie = texte libre (pas de JSON) — glossaire section (titre + gras) injecté pour le vocabulaire juridique. | — (pas d'`evals/transcription/` : le spike bloc 7.0 a validé la faisabilité sur un extrait réel ; un jeu d'or nécessite plusieurs enregistrements réels variés, hors scope de ce bloc) |
| 2026-07-09 | `feynman_turn.v1.md` | Version initiale (bloc 7.2) : appel L4, sortie = texte libre STREAMÉ (relance socratique, jamais d'explication). Fichier distinct de `feynman_bilan.v1.md` bien que les deux partagent le même `appel` PromptLog ("feynman", un seul modèle configuré pour les deux) — formes de requête/réponse trop différentes (texte streamé vs JSON structuré) pour un seul fichier (DECISIONS.md bloc 7.2). | — (pas d'`evals/feynman/` : nécessite de vrais dialogues Feynman, produits par un usage réel de l'app à partir de ce bloc, cf. CLAUDE.md §2 données réelles) |
| 2026-07-09 | `feynman_bilan.v1.md` | Version initiale (bloc 7.2) : appel L5, sortie = JSON {points {statut demontre/recite/non_abordé}, points_solides, lacunes} — verdict calculé en code (`computeFeynmanVerdict`), pas demandé au modèle, même doctrine que `correction_erreurs.v1.md`. | — (pas d'`evals/feynman/`, même raison que `feynman_turn.v1.md`) |
| 2026-07-09 | `feynman_turn.v2.md` | Incident réel signalé en usage : le modèle enchaînait systématiquement sur une nouvelle question après chaque réponse de l'étudiant, y compris quand celui-ci n'avait pas fini de développer le point en cours (donnait l'impression d'« oublier » ce qui venait d'être dit, alors que l'historique était correctement threadé — cf. aussi le vrai bug de contexte corrigé dans `src/services/session.ts`/`feynman-chat.tsx` la même session). Règle 2 ajoutée : distinguer explicitement « il reste de la marge sur ce même point → inviter à continuer, ne pas changer de sujet » de « ce point est couvert ET il en reste d'autres → nouvelle question ». | — (pas d'`evals/feynman/` : même raison que v1, toujours pas de jeu d'or ; vérification par relecture du prompt + prochain usage réel) |
